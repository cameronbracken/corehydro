% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toolbox.R
\name{qr_solve}
\alias{qr_solve}
\title{Solve a linear system by QR decomposition}
\usage{
qr_solve(a, b)
}
\arguments{
\item{a}{a numeric matrix, M x N.}

\item{b}{a numeric vector of length M, or a numeric matrix with M rows.}
}
\value{
a numeric vector of length N when \code{b} is a vector, or an \verb{N x ncol(b)} matrix when
\code{b} is a matrix.
}
\description{
Mirrors the C# \code{QRDecomposition::Solve} overloads (vector and matrix right-hand sides).
\code{a} need not be square: an overdetermined system is solved in the least-squares sense; an
underdetermined system leaves the trailing \code{ncol(a) - min(nrow(a), ncol(a))} unknowns at
zero (matching \code{QRDecomposition.Solve}'s own truncated back-substitution, which only ever
fills indices below \code{min(m, n)}).
}
\examples{
a <- matrix(c(1, 2, 3, 0, 1, 4, 5, 6, 0), nrow = 3, byrow = TRUE)
qr_solve(a, c(1, 2, 3))
}
