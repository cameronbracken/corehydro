% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mvdist.R
\name{mvdist_inverse_cdf}
\alias{mvdist_inverse_cdf}
\title{Multivariate inverse CDF (Cholesky map)}
\usage{
mvdist_inverse_cdf(mv, p)
}
\arguments{
\item{mv}{a \code{corehydro_mvdist} of family \code{"MultivariateNormal"} or \code{"MultivariateStudentT"}.}

\item{p}{numeric vector of probabilities in \verb{(0, 1)} (see Details for the required length).}
}
\value{
a numeric vector of length \code{\link[=mvdist_dimension]{mvdist_dimension()}}.
}
\description{
Maps a vector of independent uniform draws into one point on the distribution's scale via the
Cholesky decomposition of its covariance. \strong{This is not a true multivariate quantile} (there
is no unique multivariate analogue of the univariate inverse CDF); it is exactly the map
upstream implements: \code{mean + L \%*\% qnorm(p)} where \code{L} is the Cholesky factor.
}
\details{
\code{MultivariateNormal} takes \code{dimension} probabilities; \code{MultivariateStudentT} takes
\code{dimension + 1} (the last value drives the chi-squared mixing variable) and still returns
\code{dimension} values.
}
\examples{
mvdist_inverse_cdf(mvdist_normal(c(0, 0), diag(2)), c(0.5, 0.5))
}
