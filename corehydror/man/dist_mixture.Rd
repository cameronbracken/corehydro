% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distribution.R
\name{dist_mixture}
\alias{dist_mixture}
\title{Mixture distribution}
\usage{
dist_mixture(components, weights, zero_inflated = FALSE, zero_weight = 0)
}
\arguments{
\item{components}{a list of \code{corehydro_dist} objects, the mixture components.}

\item{weights}{numeric vector of mixture weights, the same length as \code{components}.}

\item{zero_inflated}{whether the mixture places extra probability mass at zero.}

\item{zero_weight}{the probability mass at zero when \code{zero_inflated} is \code{TRUE}; ignored
otherwise.}
}
\value{
a \code{corehydro_dist} of family \code{"Mixture"}, accepted by every \verb{dist_*()} verb.
}
\description{
A weighted mixture of component distributions, optionally zero-inflated. Mirrors the
C# \code{Mixture} composite.
}
\examples{
d <- dist_mixture(
  list(distribution("Normal", c(0, 1)), distribution("Normal", c(5, 1))),
  weights = c(0.5, 0.5)
)
dist_pdf(d, 2.5)
}
