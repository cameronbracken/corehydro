% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit_diagnostics}
\alias{fit_diagnostics}
\title{Estimation diagnostics for a fit}
\usage{
fit_diagnostics(fit)
}
\arguments{
\item{fit}{a \code{corehydro_fit} from \code{\link[=fit_map]{fit_map()}}, \code{\link[=fit_bayesian]{fit_bayesian()}}, or \code{\link[=fit_gmm]{fit_gmm()}}. \code{\link[=fit_mle]{fit_mle()}}
fits are not supported: \code{MaximumLikelihood} has no posterior to diagnose.}
}
\value{
A named list. \verb{$cooks_distance} and \verb{$leverage} (one value per observation) and
\verb{$observation_influence} (an observations-by-parameters matrix) are populated for
\code{\link[=fit_map]{fit_map()}} and \code{\link[=fit_gmm]{fit_gmm()}} fits. \verb{$pareto_k} (one per observation), \verb{$max_pareto_k}, and
\verb{$prior_influence}/\verb{$prior_influence_names} (one per parameter) are populated for
\code{\link[=fit_bayesian]{fit_bayesian()}} fits. Fields the fit's target does not support come back empty.
}
\description{
Compute the estimation diagnostics available for a fit: Cook's distance, per-observation
leverage, and observation influence (from the objective's Hessian at the fitted point) for
\code{\link[=fit_map]{fit_map()}} and \code{\link[=fit_gmm]{fit_gmm()}} fits; leverage, PSIS-LOO Pareto-k, and prior influence for
\code{\link[=fit_bayesian]{fit_bayesian()}} fits. Wraps the shared C++ Diagnostics layer (\code{LeverageDiagnostics},
\code{InfluenceDiagnostics}, \code{PriorInfluenceDiagnostics}).
}
\details{
Reruns the fit's own construct through the corresponding C++ diagnostics method rather than
reusing anything cached on \code{fit} -- for a \code{\link[=fit_bayesian]{fit_bayesian()}} result this reproduces the identical
seeded chain (the construct carries the same explicit \code{warmup}/\code{seed}/... the original fit
used), so the diagnostics agree with the fit they were computed from.
}
\examples{
peaks <- c(12500, 15300, 8900, 22100, 18700, 14200, 9800, 28500, 17400, 11600)
d <- fit_diagnostics(fit_map(model_univariate("Normal", peaks)))
d$cooks_distance
}
\seealso{
\code{\link[=fit_map]{fit_map()}}, \code{\link[=fit_bayesian]{fit_bayesian()}}, \code{\link[=fit_gmm]{fit_gmm()}}, \code{\link[=estimation_diagnostics]{estimation_diagnostics()}}.
}
