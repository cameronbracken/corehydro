% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{spatial_gev_analysis}
\alias{spatial_gev_analysis}
\title{Hierarchical spatial-GEV frequency analysis}
\usage{
spatial_gev_analysis(
  coordinates,
  at_site_data,
  cross_validation = FALSE,
  sampler = "DEMCz",
  iterations = 3000L,
  output_length = 10000L,
  credible_level = 0.9,
  seed = 12345L,
  number_of_chains = 4L,
  exceedance_probabilities = NULL,
  thinning_interval = -1L
)
}
\arguments{
\item{coordinates}{numeric matrix (or list of length-2 vectors), one \verb{[x, y]} row per site.}

\item{at_site_data}{numeric matrix (or list of rows), \verb{[observations x sites]} at-site maxima.}

\item{cross_validation}{logical; run leave-one-site-out cross-validation (default \code{FALSE}).}

\item{sampler}{MCMC sampler: \code{"DEMCz"} (default), \code{"DEMCzs"}, \code{"ARWMH"}, or \code{"NUTS"}.}

\item{iterations}{number of post-warmup MCMC iterations.}

\item{output_length}{number of posterior samples used to build the credible band.}

\item{credible_level}{credible-interval width (e.g. \code{0.90} for a 90\% band).}

\item{seed}{PRNG seed for the sampler (fixed for reproducibility).}

\item{number_of_chains}{number of MCMC chains (default \code{4}).}

\item{exceedance_probabilities}{optional numeric vector of exceedance probabilities at which to
tabulate the curve; when \code{NULL}, the 25 standard default ordinates are used. Must be in
\strong{ascending} order (as the defaults are); an unsorted vector fails validation and the
analysis reports itself as not valid.}

\item{thinning_interval}{MCMC thinning interval; \code{-1} (default) keeps the sampler's own default.}
}
\value{
A named list: the regional \code{parameters}/\code{mode_curve}/\code{mean_curve}/\code{lower_ci}/\code{upper_ci}
\itemize{
\item \code{aic}/\code{bic}/\code{dic}, \code{site_count}, the per-site \code{site_location_mean}/\verb{_lower}/\verb{_upper} (and the
scale/shape analogues), site-0 \code{site0_quantile_mean}/\verb{_lower}/\verb{_upper}/\verb{_mode}, and (when
\code{cross_validation}) \code{cv_site_prediction_errors}/\code{cv_site_rmse}/\code{cv_site_bias}/\code{cv_mae}/
\code{cv_rmse}/\code{cv_mean_bias}.
}
}
\description{
Fit the hierarchical spatial-GEV model over a set of gauged sites with a Bayesian MCMC and
return the regional (site-averaged) frequency curve plus per-site GEV/quantile credible bands.
Wraps the shared C++ \code{SpatialGEVAnalysis}.
}
