% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toolbox.R
\name{sobol_sequence}
\alias{sobol_sequence}
\title{Sobol quasi-random low-discrepancy sequence}
\usage{
sobol_sequence(n, dimension = 1L, skip = 0L)
}
\arguments{
\item{n}{number of points to generate, at least 1.}

\item{dimension}{spatial dimension, between 1 and 21201. Default 1.}

\item{skip}{number of points to skip before the first returned point: \code{skip = k} returns the
same first point as the C# \code{SkipTo(k)} call, i.e. the sequence's \code{(k + 1)}-th point.
Default 0 (no skip).}
}
\value{
an \code{n} by \code{dimension} numeric matrix, every value in \verb{[0, 1)}.
}
\description{
Mirrors the C# \code{SobolSequence} class. \code{dimension > 1} needs the new-joe-kuo-6 direction
numbers shipped with the package (\code{dimension == 1} needs no file, matching the C# ctor); the
installed file is located automatically with \code{\link[=system.file]{system.file()}}.
}
\examples{
sobol_sequence(5, dimension = 2)
}
