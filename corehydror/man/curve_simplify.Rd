% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toolbox.R
\name{curve_simplify}
\alias{curve_simplify}
\title{Simplify a paired x-y curve}
\usage{
curve_simplify(
  x,
  y,
  method = "rdp",
  tolerance = NULL,
  num_to_keep = NULL,
  look_ahead = NULL,
  order_x = "ascending",
  order_y = "ascending",
  strict_x = TRUE,
  strict_y = TRUE
)
}
\arguments{
\item{x, y}{numeric vectors of equal length (at least two), the curve's ordinates.}

\item{method}{one of \code{"rdp"} (default), \code{"visvalingam"}, or \code{"lang"}.}

\item{tolerance}{perpendicular-distance tolerance; required for \code{method} \code{"rdp"} or \code{"lang"}.}

\item{num_to_keep}{number of points to keep; required for \code{method = "visvalingam"}, and must be
at least 2 (the algorithm always keeps the curve's first and last point, and needs at least
3 ordinates to triangulate at every intermediate step).}

\item{look_ahead}{the Lang algorithm's look-ahead window; required for \code{method = "lang"}.}

\item{order_x, order_y}{one of \code{"ascending"} (default), \code{"descending"}, or \code{"none"}.}

\item{strict_x, strict_y}{require x/y to strictly increase/decrease (per \code{order_x}/\code{order_y})
between consecutive ordinates. Default \code{TRUE}.}
}
\value{
a data frame with columns \code{x} and \code{y}.
}
\description{
Mirrors the C# \code{OrderedPairedData}'s three curve-simplification algorithms: Douglas-Peucker
(\code{method = "rdp"}, needs \code{tolerance}), Visvalingam-Whyatt (\code{method = "visvalingam"}, needs
\code{num_to_keep}), and Lang (\code{method = "lang"}, needs \code{tolerance} and \code{look_ahead}). NOTE: unlike
\code{rdp}/\code{visvalingam}, which always keep the curve's first and last point, \code{lang} does not
force-keep the trailing point -- a real, verified-against-the-real-C#-library upstream
behavior (see \code{ordered_paired_data.hpp}'s sixth transcription note), not a port bug.
}
\examples{
x <- c(0, 1.57, 3.14, 4.71, 6.28)
y <- c(0, 1, 0, -1, 0)
curve_simplify(x, y, method = "rdp", tolerance = 0.01, strict_y = FALSE, order_y = "none")
}
