% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toolbox.R
\name{trend_predict}
\alias{trend_predict}
\alias{trend_parameters}
\title{Evaluate a trend model}
\usage{
trend_predict(tr, index)

trend_parameters(tr)
}
\arguments{
\item{tr}{a \code{corehydro_trend} object from \code{\link[=trend]{trend()}}.}

\item{index}{a 1-based numeric vector of time indices to evaluate at.}
}
\value{
a numeric vector, the same length as \code{index}.

\code{trend_parameters()} returns a named numeric vector, the trend's own model-parameter
values (see \code{\link[=model_parameter]{model_parameter()}}) at their class defaults, adjusted by any explicit \code{values}
on \code{tr}.
}
\description{
Builds the trend model named by \code{tr} (a \code{\link[=trend]{trend()}} object) from its own class defaults, then
evaluates it at \code{index}. \code{tr}'s \code{parameter} field (which distribution parameter the trend
attaches to) is not used here -- this verb evaluates the trend model on its own, the same way
\code{set_trend_model()}'s type dispatch does before it further data-drives the parameter values
from a live model's distribution and data (this verb has no such model to consult, so a
\code{tr} with no explicit \code{values} predicts from the trend's own zero-valued class defaults).
}
\examples{
tr <- trend("location", "Linear", start_index = 0, values = c(10, 2))
trend_predict(tr, 1:5)
trend_parameters(trend("location", "Linear", values = c(10, 2)))
}
