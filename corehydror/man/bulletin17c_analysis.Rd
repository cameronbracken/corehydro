% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{bulletin17c_analysis}
\alias{bulletin17c_analysis}
\title{Bulletin 17C flood-frequency analysis}
\usage{
bulletin17c_analysis(
  data,
  uncertainty_method = "MultivariateNormal",
  output_length = 10000L,
  seed = 12345L,
  confidence_level = 0.9,
  exceedance_probabilities = NULL
)
}
\arguments{
\item{data}{numeric vector of annual peak observations, or a \code{\link[=model_bulletin17c]{model_bulletin17c()}} model. A
model can bring historical and paleoflood interval observations, perception thresholds, and
Multiple Grubbs-Beck low outliers (see \code{\link[=analysis_data]{analysis_data()}}), which is the case Bulletin 17C
exists for.}

\item{uncertainty_method}{uncertainty-quantification method: \code{"MultivariateNormal"} (default),
\code{"Bootstrap"}, \code{"LinkedMultivariateNormal"}, or \code{"BiasCorrectedBootstrap"}.}

\item{output_length}{number of parameter-set draws used for uncertainty quantification.}

\item{seed}{PRNG seed for the uncertainty draw.}

\item{confidence_level}{confidence level for the intervals (e.g. \code{0.90}).}

\item{exceedance_probabilities}{optional numeric vector of exceedance probabilities; when
\code{NULL}, the 25 standard default ordinates are used. Must be in \strong{ascending} order.}
}
\value{
A named list: \code{exceedance_probabilities}, \code{point_estimates} (log10 space), \code{lower_ci},
\code{upper_ci} (discharge space), \code{confidence_level}, \code{beta1}, \code{nu}, \code{quantile_variance},
\code{parameters} (fitted location/scale/shape), and \code{covariance} (the p x p sandwich covariance).
}
\description{
Fit a Bulletin 17C (log-Pearson Type III) flood-frequency model by the generalized method of
moments and return the Cohn-style delta-method confidence intervals, the fitted parameters, and
the sandwich covariance. Wraps the shared C++ \code{Bulletin17CAnalysis}.
}
\examples{
peaks <- c(12500, 15300, 8900, 22100, 18700, 14200, 9800, 28500, 17400, 11600,
           19200, 13800, 25600, 10500, 16900)
ci <- bulletin17c_analysis(peaks, output_length = 200, seed = 12345)
ci$confidence_level

# With Multiple Grubbs-Beck low-outlier censoring, through a model.
m <- model_bulletin17c(analysis_data(peaks, mgbt_low_outliers = TRUE))
bulletin17c_analysis(m, output_length = 200, seed = 12345)$confidence_level
}
