% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit_mle}
\alias{fit_mle}
\title{Maximum likelihood fit}
\usage{
fit_mle(
  model,
  distribution = NULL,
  optimizer = "NelderMead",
  hessian = TRUE,
  profile = FALSE,
  profile_bins = 100
)
}
\arguments{
\item{model}{a \code{\link[=model_univariate]{model_univariate()}} (or any \verb{model_*()}) object, or a plain numeric vector of
observations together with \code{distribution}. A model can bring censored observations (see
\code{\link[=analysis_data]{analysis_data()}}), nonstationary trends (see \code{\link[=trend]{trend()}}), and parameter bounds or priors
(see \code{\link[=model_parameter]{model_parameter()}}).}

\item{distribution}{distribution family name, required only when \code{model} is a numeric vector.}

\item{optimizer}{one of \code{"NelderMead"} (default), \code{"Brent"}, \code{"BFGS"}, \code{"Powell"},
\code{"DifferentialEvolution"}, \code{"MultilevelSingleLinkage"}.}

\item{hessian}{logical; compute the covariance, standard errors and correlation. \code{TRUE} by
default. A model with fewer than two parameters reports \code{NA} for all three.}

\item{profile}{logical; also compute the profile likelihood and profile confidence intervals.
\code{FALSE} by default because each costs \code{profile_bins * length(parameters)} likelihood
evaluations.}

\item{profile_bins}{number of bins in each parameter's profile.}
}
\value{
An object of class \code{corehydro_fit}. When \code{profile = TRUE}, it also carries \verb{$profile},
a named list (one entry per parameter, named to match \code{coef()}) of \code{profile_bins}-by-2
matrices with columns \code{value} and \code{log_likelihood}, the profile-likelihood grid \code{confint()}'s
intervals are drawn from -- plot a parameter's curve with e.g.
\code{plot(f$profile[[1]], type = "l")}. Absent (\code{NULL}) when the fit was built with the default
\code{profile = FALSE}. See \code{\link[=fit_bayesian]{fit_bayesian()}} for the Bayesian surface.
}
\description{
Fit a model by maximum likelihood and return a fit object carrying the parameter estimates,
the Hessian-based covariance, and optimizer bookkeeping. Wraps the shared C++
\code{MaximumLikelihood} ported from USACE-RMC RMC.BestFit.
}
\examples{
peaks <- c(12500, 15300, 8900, 22100, 18700, 14200, 9800, 28500, 17400, 11600)
f <- fit_mle(model_univariate("LogPearsonTypeIII", peaks))
coef(f)
AIC(f)
}
\seealso{
\code{\link[=fit_map]{fit_map()}}, \code{\link[=fit_bayesian]{fit_bayesian()}}, \code{\link[=fit_gmm]{fit_gmm()}}, \code{\link[=fit_diagnostics]{fit_diagnostics()}}.
}
