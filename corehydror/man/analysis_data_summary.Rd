% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{analysis_data_summary}
\alias{analysis_data_summary}
\title{Summarize an observation frame}
\usage{
analysis_data_summary(data, plotting_parameter = 0)
}
\arguments{
\item{data}{a \code{corehydro_data} object, or a numeric vector of observations.}

\item{plotting_parameter}{the plotting-position parameter in \verb{[0, 1)}: \code{0} (the default) is
Weibull, \code{0.40} Cunnane, \code{0.44} Gringorten, \code{0.50} Hazen.}
}
\value{
A named list. \code{index}, \code{value}, \code{plotting_position}, and \code{is_low_outlier} are parallel
vectors over the exact series; \code{number_of_low_outliers}, \code{low_outlier_threshold},
\code{plotting_parameter}, \code{lambda} (events per index), \code{total_record_length},
\code{zero_value_relative_frequency}, and the four series counts are scalars.
}
\description{
Run the plotting-position and threshold cascade over an \code{\link[=analysis_data]{analysis_data()}} frame and return the
record diagnostics. The plotting positions are the Hirsch-Stedinger censored positions, so
interval and threshold observations shift the positions of the systematic record even though
they carry no plotting ordinate themselves.
}
\examples{
peaks <- c(12500, 15300, 8900, 22100, 18700, 14200, 9800, 28500)
s <- analysis_data_summary(analysis_data(peaks))
data.frame(value = s$value, position = s$plotting_position)

# A perception threshold reweights the systematic record's positions.
d <- analysis_data(
  exact = peaks,
  threshold = data.frame(start_index = 8, end_index = 47, value = 25000, number_above = 0)
)
analysis_data_summary(d)$plotting_position
}
