% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toolbox.R
\name{tabular_function}
\alias{tabular_function}
\title{Evaluate a tabular function}
\usage{
tabular_function(
  x,
  distributions,
  at,
  inverse = FALSE,
  x_transform = c("none", "logarithmic", "normal_z"),
  y_transform = c("none", "logarithmic", "normal_z"),
  confidence_level = NULL,
  allow_negative_y_values = FALSE
)
}
\arguments{
\item{x}{numeric vector, the curve's x positions, at least one element.}

\item{distributions}{a \code{\link[=distribution]{distribution()}} object, or a list of them the same length as \code{x} --
one distribution per x position. A single distribution is recycled across every \code{x}.}

\item{at}{numeric vector of points to evaluate at (or, when \code{inverse = TRUE}, points to
evaluate the inverse function at).}

\item{inverse}{if \code{TRUE}, evaluates \code{InverseFunction()} instead of \code{Function()}.}

\item{x_transform, y_transform}{one of \code{"none"} (default), \code{"logarithmic"}, or \code{"normal_z"}.}

\item{confidence_level}{quantile in \verb{[0, 1]} to sample the curve at; \code{NULL} (default) samples
the mean.}

\item{allow_negative_y_values}{allow a negative or \code{NaN} result to pass through unmodified,
rather than clamping it to \code{0}. Default \code{FALSE} (clamp), matching every use in the ported
C# test suite -- the C# class default is \code{TRUE}.}
}
\value{
a numeric vector, the same length as \code{at}.
}
\description{
Mirrors the C# \code{TabularFunction}: builds an uncertain paired curve from \code{x} and
\code{distributions}, samples it once (the mean curve, or \code{confidence_level} if given), and
evaluates \code{Function()}/\code{InverseFunction()} at \code{at}. Unlike \code{\link[=curve_interpolate]{curve_interpolate()}} and friends,
the underlying curve's shape contract is not configurable here -- \code{TabularFunction} is always
built strict, ascending on both axes, matching every use in the ported C# test suite.
}
\examples{
x <- c(50, 100, 150, 200, 250)
d <- lapply(c(100, 200, 300, 400, 500), function(v) distribution("Deterministic", v))
tabular_function(x, d, at = 50, x_transform = "logarithmic")
}
