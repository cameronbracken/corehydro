% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/timeseries.R
\name{ts_moving_average}
\alias{ts_moving_average}
\alias{ts_moving_sum}
\title{Moving average and moving sum}
\usage{
ts_moving_average(ts, period, min_valid_count = NULL)

ts_moving_sum(ts, period, min_valid_count = NULL)
}
\arguments{
\item{ts}{a \code{corehydro_ts}.}

\item{period}{the window length, which must be shorter than the series.}

\item{min_valid_count}{the minimum number of observed values a window needs. Default \code{NULL}
(strict).}
}
\value{
a \code{corehydro_ts}.
}
\description{
Trailing windows over the previous \code{period} ordinates, mirroring the C# \code{MovingAverage} and
\code{MovingSum}. The result is shorter than the input by \code{period - 1}, and each ordinate carries
the date of its window's LAST observation.
}
\details{
\code{min_valid_count} controls what a window holding missing values does. The default -- \code{NULL},
meaning \code{period} -- propagates strictly: any missing value in the window gives a missing
result, which matches \code{pandas}'s default \code{min_periods}. A smaller value averages (or sums) the
observed entries only; no rescaling is applied to a sum.
}
\examples{
ts <- time_series(as.Date("2000-01-01") + 0:9, c(3, 1, 4, 1, 5, 9, 2, 6, 5, 3))
ts_moving_average(ts, period = 3)
}
