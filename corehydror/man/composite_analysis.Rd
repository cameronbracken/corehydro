% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{composite_analysis}
\alias{composite_analysis}
\title{Composite frequency analysis}
\usage{
composite_analysis(
  data,
  families,
  composite_type = "CompetingRisks",
  average_method = "AIC",
  sampler = "DEMCz",
  iterations = 3000L,
  output_length = 10000L,
  credible_level = 0.9,
  seed = 12345L,
  exceedance_probabilities = NULL,
  thinning_interval = -1L
)
}
\arguments{
\item{data}{numeric vector of observations shared by every child analysis, or an
\code{\link[=analysis_data]{analysis_data()}} frame. A frame is cloned into every child fit, so a censored record stays
censored throughout.}

\item{families}{character vector of child distribution family names (one child analysis each).}

\item{composite_type}{\code{"CompetingRisks"} (default), \code{"Mixture"}, or \code{"ModelAverage"}.}

\item{average_method}{model-averaging criterion when \code{composite_type = "ModelAverage"}:
\code{"AIC"} (default), \code{"BIC"}, \code{"DIC"}, \code{"WAIC"}, \code{"LOOIC"}, \code{"Equal"}, or \code{"RMSE"}.}

\item{sampler}{MCMC sampler: \code{"DEMCz"} (default), \code{"DEMCzs"}, \code{"ARWMH"}, or \code{"NUTS"}.}

\item{iterations}{number of post-warmup MCMC iterations.}

\item{output_length}{number of posterior samples used to build the credible band.}

\item{credible_level}{credible-interval width (e.g. \code{0.90} for a 90\% band).}

\item{seed}{PRNG seed for the sampler (fixed for reproducibility).}

\item{exceedance_probabilities}{optional numeric vector of exceedance probabilities at which to
tabulate the curve; when \code{NULL}, the 25 standard default ordinates are used. Must be in
\strong{ascending} order (as the defaults are); an unsorted vector fails validation and the
analysis reports itself as not valid.}

\item{thinning_interval}{MCMC thinning interval; \code{-1} (default) keeps the sampler's own default.}
}
\value{
A named list: \code{parameters}, \code{mode_curve}, \code{mean_curve}, \code{lower_ci}, \code{upper_ci}, and the
scalars \code{aic}, \code{bic}, \code{dic}, \code{rmse}.
}
\description{
Combine several fitted univariate frequency analyses (one per \code{families} entry, each fit to
\code{data} by a Bayesian MCMC) into a single composite frequency curve via competing-risks, mixture,
or model-averaging aggregation. Wraps the shared C++ \code{CompositeAnalysis}.
}
