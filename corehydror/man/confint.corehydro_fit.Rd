% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{confint.corehydro_fit}
\alias{confint.corehydro_fit}
\title{Profile-likelihood confidence intervals for a fit}
\usage{
\method{confint}{corehydro_fit}(object, parm, level = 0.95, ...)
}
\arguments{
\item{object}{a \code{corehydro_fit} from \code{\link[=fit_mle]{fit_mle()}} or \code{\link[=fit_map]{fit_map()}}.}

\item{parm}{optional subset of parameters (by name or position); every parameter by default.}

\item{level}{confidence level.}

\item{...}{unused; present for generic consistency.}
}
\value{
A matrix with one row per parameter and columns \code{lower}/\code{upper}.
}
\description{
Confidence intervals from the profile likelihood, at the requested level. When the fit already
carries a profile block at the requested level (built with \code{fit_mle(..., profile = TRUE)} or a
prior \code{confint()} call), those bounds are reused; otherwise the identical fit is re-run with
profiling turned on, following the lazy-rebuild precedent of \code{ch_estimation_bic_}
(\code{corehydror/src/estimation.cpp}) -- a deterministic optimizer reproduces the identical point
estimate, so the rebuild's confidence intervals are the ones the original fit would have
carried had \code{profile = TRUE} been requested at the matching level up front.
}
\examples{
peaks <- c(12500, 15300, 8900, 22100, 18700, 14200, 9800, 28500, 17400, 11600)
f <- fit_mle(model_univariate("Normal", peaks))
confint(f, level = 0.9)
}
