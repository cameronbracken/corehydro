% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{confint.corehydro_fit}
\alias{confint.corehydro_fit}
\title{Confidence or credible intervals for a fit}
\usage{
\method{confint}{corehydro_fit}(object, parm, level = 0.95, ...)
}
\arguments{
\item{object}{a \code{corehydro_fit} from \code{\link[=fit_mle]{fit_mle()}}, \code{\link[=fit_map]{fit_map()}}, or \code{\link[=fit_bayesian]{fit_bayesian()}}.}

\item{parm}{optional subset of parameters (by name or position); every parameter by default.}

\item{level}{confidence (MLE/MAP) or credible (Bayesian) level.}

\item{...}{unused; present for generic consistency.}
}
\value{
A matrix with one row per parameter and columns \code{lower}/\code{upper}.
}
\description{
\code{\link[=fit_mle]{fit_mle()}} and \code{\link[=fit_map]{fit_map()}} fits get profile-likelihood confidence intervals; a \code{\link[=fit_bayesian]{fit_bayesian()}}
fit gets posterior credible intervals. \code{\link[=fit_gmm]{fit_gmm()}} has no interval surface (see below).
}
\details{
For \code{\link[=fit_mle]{fit_mle()}}/\code{\link[=fit_map]{fit_map()}}: when the fit already carries a profile block at the requested
level (built with \code{fit_mle(..., profile = TRUE)} or a prior \code{confint()} call), those bounds are
reused; otherwise the identical fit is re-run with profiling turned on, following the
lazy-rebuild precedent of \code{ch_estimation_bic_} (\code{corehydror/src/estimation.cpp}) -- a
deterministic optimizer reproduces the identical point estimate, so the rebuild's confidence
intervals are the ones the original fit would have carried had \code{profile = TRUE} been requested
at the matching level up front.

For \code{\link[=fit_bayesian]{fit_bayesian()}}: \verb{$summary}'s \code{lower}/\code{upper} columns are already the posterior credible
interval at the fit's own \verb{$credible_level} (the \code{credible_level} argument, 0.9 by default).
When the requested \code{level} matches, those columns are returned directly; otherwise the
identical seeded chain is re-run with \code{credible_interval_width} set to \code{level} (the same
lazy-rebuild precedent as the profile path above -- re-sampling with an identical seed
reproduces the same chain bit-for-bit, so only the post-hoc credible-interval quantile
computation changes). Because \code{confint()}'s own default is 0.95 (the base R \code{confint}
convention) and a fit's default is 0.9, a bare \code{confint(f)} on a Bayesian fit re-runs the
chain; \code{fit_bayesian(..., credible_level = 0.95)} avoids that.

\code{\link[=fit_gmm]{fit_gmm()}} is method-of-moments and has no likelihood or posterior to draw an interval from;
calling \code{confint()} on one errors. Use \code{\link[=quantile_variance]{quantile_variance()}} for the delta-method variance of a
fitted quantile instead.
}
\examples{
peaks <- c(12500, 15300, 8900, 22100, 18700, 14200, 9800, 28500, 17400, 11600)
f <- fit_mle(model_univariate("Normal", peaks))
confint(f, level = 0.9)
}
\seealso{
\code{\link[=fit_mle]{fit_mle()}}, \code{\link[=fit_map]{fit_map()}}, \code{\link[=fit_bayesian]{fit_bayesian()}}, \code{\link[=fit_gmm]{fit_gmm()}}, \code{\link[=quantile_variance]{quantile_variance()}}.
}
