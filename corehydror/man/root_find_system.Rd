% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/callback.R
\name{root_find_system}
\alias{root_find_system}
\title{Solve a system of nonlinear equations}
\usage{
root_find_system(
  f,
  jacobian,
  first_guess,
  tolerance = NULL,
  max_iterations = NULL
)
}
\arguments{
\item{f}{the system of equations: a function taking a numeric vector and returning a numeric
vector of the same length.}

\item{jacobian}{the Jacobian of \code{f}: a function taking the same numeric vector and returning the
square matrix of partial derivatives, one ROW per equation.}

\item{first_guess}{the starting vector; its length fixes the dimension of the system.}

\item{tolerance}{the convergence tolerance, applied to both the step size and the residual.
\code{NULL}, the default, leaves the ported solver's own default (1e-8) in force.}

\item{max_iterations}{the iteration cap; the search raises an error if it is reached. \code{NULL},
the default, leaves the ported solver's own default (1000) in force.}
}
\value{
the root, a numeric vector the length of \code{first_guess}.
}
\description{
Solves \code{F(x) = 0} for a vector-valued \code{F} with the ported Numerics multivariate Newton-Raphson
method, iterating \verb{x_(n+1) = x_n - J(x_n)^-1 F(x_n)}.
}
\examples{
f <- function(v) c(3 * v[1] + v[2] - 9, v[1] + 2 * v[2] - 8)
j <- function(v) matrix(c(3, 1, 1, 2), nrow = 2, byrow = TRUE)
root_find_system(f, j, first_guess = c(0, 0))
}
