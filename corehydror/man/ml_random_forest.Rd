% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml.R
\name{ml_random_forest}
\alias{ml_random_forest}
\title{Random forest regression or classification}
\usage{
ml_random_forest(
  x,
  y,
  newdata,
  seed = NULL,
  regression = TRUE,
  features = NULL,
  minimum_split_size = 2,
  max_depth = 100,
  number_of_trees = 1000,
  alpha = 0.1
)
}
\arguments{
\item{x}{a numeric matrix or data frame of training predictors, one row per observation.}

\item{y}{the training response, one value per row of \code{x}.}

\item{newdata}{predictors to predict for, with the same number of columns as \code{x}.}

\item{seed}{integer PRNG seed; \code{NULL} uses the computer clock.}

\item{regression}{\code{TRUE} (the default) fits regression trees; \code{FALSE} classifiers.}

\item{features}{the number of random features per split. \code{NULL} uses \code{max(1, ncol(x) - 1)}.}

\item{minimum_split_size}{the smallest node a tree will split. Default 2.}

\item{max_depth}{the recursion cap. Default 100.}

\item{number_of_trees}{how many trees to grow. Default 1000.}

\item{alpha}{the interval level: 0.1 (the default) gives a 90\% interval.}
}
\value{
a matrix with one row per row of \code{newdata} and columns \code{lower}, \code{median}, \code{upper},
\code{mean}.
}
\description{
Mirrors the C# \code{RandomForest} class: fits \code{number_of_trees} decision trees on bootstrap
resamples of the training data and reports the spread of their predictions as an interval.
}
\details{
Training cost is linear in \code{number_of_trees}, and the library's default of 1000 is the knob to
turn if a call is slow -- a few dozen trees is usually enough to see the shape of the answer.
A seeded run is bit-identical between R and Python, because the whole computation lives in the
shared compiled core.

For a classifier every column is floored to an integer class label, including \code{mean}.
}
\examples{
x <- c(1, 2, 3, 4, 5, 6, 100, 101, 102, 103, 104, 105)
y <- c(10, 11, 10, 11, 10, 11, 100, 101, 100, 101, 100, 101)
ml_random_forest(x, y, newdata = c(3, 104), seed = 42, number_of_trees = 25)
}
