% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mvdist.R
\name{mvdist_random}
\alias{mvdist_random}
\title{Draw from a multivariate distribution}
\usage{
mvdist_random(mv, n, seed = NULL, method = c("random", "latin_hypercube"))
}
\arguments{
\item{mv}{a \code{corehydro_mvdist}.}

\item{n}{number of draws.}

\item{seed}{integer seed for reproducible draws; \code{NULL} (the default) seeds from the clock,
except for \code{method = "latin_hypercube"} where it is required.}

\item{method}{\code{"random"} (the default) for ordinary Monte Carlo, or \code{"latin_hypercube"}.}
}
\value{
an \verb{n x dimension} numeric matrix.
}
\description{
Simulate from the distribution's own seeded Mersenne Twister stream. A given \code{seed}
reproduces the same draws bit-for-bit in R, Python, and the upstream C# library.
\code{method = "latin_hypercube"} draws a Latin hypercube sample instead of ordinary Monte Carlo
and is available for \code{"MultivariateNormal"} and \code{"MultivariateStudentT"} only; it requires an
explicit \code{seed} (there is no clock-seeded LHS upstream).
}
\examples{
mvdist_random(mvdist_normal(c(0, 0), diag(2)), 5, seed = 12345)
}
