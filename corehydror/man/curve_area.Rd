% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toolbox.R
\name{curve_area}
\alias{curve_area}
\title{Area under a paired x-y curve}
\usage{
curve_area(
  x,
  y,
  under = c("y", "x"),
  order_x = c("ascending", "descending", "none"),
  order_y = c("ascending", "descending", "none"),
  strict_x = TRUE,
  strict_y = TRUE
)
}
\arguments{
\item{x, y}{numeric vectors of equal length (at least two), the curve's ordinates.}

\item{under}{one of \code{"y"} (default, area under the curve against the x-axis) or \code{"x"} (area
against the y-axis).}

\item{order_x, order_y}{one of \code{"ascending"} (default), \code{"descending"}, or \code{"none"}.}

\item{strict_x, strict_y}{require x/y to strictly increase/decrease (per \code{order_x}/\code{order_y})
between consecutive ordinates. Default \code{TRUE}.}
}
\value{
a single number.
}
\description{
Mirrors the C# \code{OrderedPairedData.TrapezoidalAreaUnderY}/\code{TrapezoidalAreaUnderX}: the
trapezoidal-rule area between the curve and the x-axis (\code{under = "y"}) or the y-axis
(\code{under = "x"}). Requires x (for \code{under = "y"}) or y (for \code{under = "x"}) to be sorted
ascending or descending -- \code{order_x}/\code{order_y = "none"} raises the same error the C# method
does.
}
\examples{
curve_area(c(1, 2, 3, 4), c(1, 4, 9, 16))
}
