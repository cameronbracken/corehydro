% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toolbox.R
\name{hypothesis_test}
\alias{hypothesis_test}
\title{Hypothesis tests}
\usage{
hypothesis_test(
  x = NULL,
  y = NULL,
  method = "jarque_bera",
  population_mean = 0,
  lag_max = NULL,
  index = NULL,
  sse_restricted = NULL,
  sse_full = NULL,
  df_restricted = NULL,
  df_full = NULL
)
}
\arguments{
\item{x}{numeric vector: the sample (the two-sample methods' first sample, or the response
series for \code{"linear_trend"}). Ignored for \code{"f_models"}.}

\item{y}{numeric vector, the second sample. Required for the two-sample methods listed above,
and rejected (must be \code{NULL}) for every other method -- earlier versions silently discarded
a \code{y} supplied to a one-sample method instead of raising.}

\item{method}{one of \code{"one_sample_t"}, \code{"equal_variance_t"}, \code{"unequal_variance_t"},
\code{"paired_t"}, \code{"f"}, \code{"f_models"}, \code{"jarque_bera"}, \code{"wald_wolfowitz"}, \code{"ljung_box"},
\code{"mann_whitney"}, \code{"mann_kendall"}, \code{"linear_trend"}.}

\item{population_mean}{the hypothesized mean for \code{"one_sample_t"}. Default 0.}

\item{lag_max}{the max lag for \code{"ljung_box"}. Default \code{NULL} (use the C# default rule).}

\item{index}{the index (x-axis) vector for \code{"linear_trend"}. Default \code{NULL}, meaning
\code{seq_along(x)}.}

\item{sse_restricted, sse_full, df_restricted, df_full}{the four \code{"f_models"} inputs: the
restricted and full models' sum of squared errors and degrees of freedom.}
}
\value{
a named numeric vector: \code{p_value} for every method but \code{"f_models"}, which returns
\code{c(f_statistic =, p_value =)}.
}
\description{
Mirrors the C# \code{HypothesisTests} static class: twelve one- and two-sample parametric and
nonparametric hypothesis tests, reached through the shared \code{hypothesis} toolbox group. Every
method but \code{"f_models"} returns the 2-sided p-value of its test statistic; \code{"f_models"} (the
F-test comparing two nested regression models) additionally returns the F statistic itself.
}
\details{
\code{HypothesisTests} has a thirteenth static, \code{UnimodalityTest}, that is NOT exposed here: it
trains a \code{Numerics.MachineLearning.GaussianMixtureModel} at k = 1 and k = 2, and the Machine
Learning layer has no port yet. It is deferred, not permanently out of scope -- see
\code{site/status.qmd} for the tracking note.

Argument use by method, and the C# guard each one inherits:
\itemize{
\item \code{"one_sample_t"}: \code{x}, \code{population_mean} (default 0). Needs at least 2 observations.
\item \code{"equal_variance_t"} / \code{"unequal_variance_t"}: \code{x}, \code{y}. \code{equal_variance_t} needs a
combined length of at least 3; \code{unequal_variance_t} has no length guard (upstream has none
either).
\item \code{"paired_t"}: \code{x}, \code{y}, which must be the same length.
\item \code{"f"}: \code{x}, \code{y}, each needing at least 2 observations.
\item \code{"f_models"}: \code{sse_restricted}, \code{sse_full}, \code{df_restricted}, \code{df_full} (all required; \code{x}
and \code{y} are ignored). \code{df_restricted} must differ from \code{df_full}, and \code{df_full} must be
positive.
\item \code{"jarque_bera"} / \code{"wald_wolfowitz"}: \code{x}. No length guard.
\item \code{"ljung_box"}: \code{x}, \code{lag_max} (default \code{NULL}, meaning
\code{floor(min(10 * log10(length(x)), length(x) - 1))}, the C# default rule).
\item \code{"mann_whitney"}: \code{x}, \code{y}. \code{x} must be no longer than \code{y}, each must have more than 3
observations, and the combined length must exceed 20.
\item \code{"mann_kendall"}: \code{x}. Needs at least 10 observations.
\item \code{"linear_trend"}: \code{x} (the sample), \code{index} (default \code{seq_along(x)}, i.e. \code{1:length(x)} --
a VALUE the regression is fit against, not an index into \code{x}). \code{index} and \code{x} must be the
same length.
}
}
\examples{
hypothesis_test(c(4, 5, 5, 6, 9, 12, 13, 14, 14, 19, 22, 24, 25), method = "jarque_bera")
}
