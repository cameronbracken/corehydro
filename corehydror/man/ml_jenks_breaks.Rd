% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml.R
\name{ml_jenks_breaks}
\alias{ml_jenks_breaks}
\title{Jenks natural breaks classification}
\usage{
ml_jenks_breaks(x, n_clusters, is_data_sorted = FALSE)
}
\arguments{
\item{x}{a numeric vector to classify.}

\item{n_clusters}{the number of classes. Must be at least 1 and no more than \code{length(x)}.}

\item{is_data_sorted}{set \code{TRUE} to skip the internal sort when \code{x} is already ascending.}
}
\value{
a list with \code{breaks} (each class's maximum value), \code{clusters} (an \code{n_clusters} by 8
matrix with columns \code{start_index}, \code{end_index}, \code{count}, \code{min}, \code{max}, \code{sum}, \code{average},
\code{variance}; the two index columns are 0-based positions into the SORTED data), and \code{gvf},
the goodness-of-variance-fit measure, which approaches 1 for a better fit.
}
\description{
Mirrors the C# \code{JenksNaturalBreaks} class: partitions a one-dimensional sample into
\code{n_clusters} classes minimizing the within-class sum of squared deviations.
}
\details{
Upstream fails on fully degenerate input: if every value of \code{x} is identical, the underlying
algorithm computes a negative class boundary and errors. Any spread at all is fine, including
heavily tied data.
}
\examples{
x <- c(1, 1.2, 1.4, 8, 8.3, 8.6, 30, 31, 32)
ml_jenks_breaks(x, n_clusters = 3)$breaks
}
