% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression.R
\name{linear_regression}
\alias{linear_regression}
\title{Ordinary least squares by singular value decomposition}
\usage{
linear_regression(x, y, intercept = TRUE)
}
\arguments{
\item{x}{a numeric matrix of predictors with one row per observation, or a numeric vector for
a single predictor.}

\item{y}{numeric vector of responses, one per row of \code{x}.}

\item{intercept}{whether to fit an intercept. Default \code{TRUE}.}
}
\value{
a \code{corehydro_lm} list with \code{coefficients}, \code{standard_errors}, \code{covariance},
\code{residuals}, \code{r_squared}, \code{adj_r_squared}, \code{sigma}, \code{df}, and \code{n}. \code{covariance} is the
coefficient covariance matrix, i.e. \code{sqrt(diag(covariance))} equals \code{standard_errors}; the
underlying C# \code{LinearRegression.Covariance} is the unscaled cross-product term
(\verb{(X'X)^-1}), scaled here by \code{sigma^2} to match \code{standard_errors}.
}
\description{
Mirrors the C# \code{LinearRegression} class of the Numerics library: estimates
\code{Y = alpha + beta*X + e}, \code{e ~ N(0, sigma)}, via SVD.
}
\examples{
x <- cbind(c(1, 2, 3, 4, 5), c(2, 1, 4, 3, 5))
y <- c(3.1, 4.2, 8.1, 9.2, 13.0)
fit <- linear_regression(x, y)
coef(fit)
}
\seealso{
\code{\link[=predict.corehydro_lm]{predict.corehydro_lm()}}.
}
