% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toolbox.R
\name{running_covariance}
\alias{running_covariance}
\title{Streaming covariance and correlation matrix}
\usage{
running_covariance(x, state = NULL)
}
\arguments{
\item{x}{a numeric matrix (or an object coercible to one via \code{\link[=as.matrix]{as.matrix()}}), observations in
rows and variables in columns.}

\item{state}{a \code{corehydro_running_covariance} object from a previous call, or \code{NULL} (the
default) to start a fresh accumulator.}
}
\value{
a \code{corehydro_running_covariance} list with \code{n}, \code{mean} (length-\code{ncol(x)}), and the
\code{covariance}, \code{sample_covariance}, \code{sample_correlation}, \code{population_covariance}, and
\code{population_correlation} matrices (each \code{ncol(x)} by \code{ncol(x)}). \code{covariance} is unadjusted
by sample size; \verb{sample_*}/\verb{population_*} are the N-1- and N-normalized variants.
}
\description{
Accumulates a running mean vector and covariance matrix over one or more chunks of
multivariate data, mirroring the C# \code{RunningCovarianceMatrix} class. The accumulator state
travels in the return value, so a chunked run holds no C++ state.
}
\examples{
x <- matrix(c(1, 2, 3, 4, 5, 2, 4, 5, 4, 5), ncol = 2)
running_covariance(x)
}
