% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/copula.R
\name{copula_random}
\alias{copula_random}
\title{Draw from a copula}
\usage{
copula_random(cop, n, seed = NULL)
}
\arguments{
\item{cop}{a \code{corehydro_copula}.}

\item{n}{number of draws.}

\item{seed}{integer seed for reproducible draws; \code{NULL} (the default) seeds from the clock.}
}
\value{
an \verb{n x 2} numeric matrix.
}
\description{
Simulate from the copula's own seeded Mersenne Twister stream, mapped through the attached
marginals to the data scale when both \code{margin_x} and \code{margin_y} were supplied; on the unit
square otherwise. A given \code{seed} reproduces the same draws bit-for-bit in R, Python, and the
upstream C# library.
}
\examples{
cop <- copula("Clayton", theta = 2,
              margin_x = distribution("Normal", c(0, 1)),
              margin_y = distribution("Normal", c(0, 1)))
copula_random(cop, 5, seed = 12345)
}
