% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression.R
\name{predict.corehydro_lm}
\alias{predict.corehydro_lm}
\title{Predict from a fitted linear regression}
\usage{
\method{predict}{corehydro_lm}(object, newdata, interval = FALSE, level = 0.9, ...)
}
\arguments{
\item{object}{a \code{corehydro_lm} from \code{\link[=linear_regression]{linear_regression()}}.}

\item{newdata}{a numeric matrix of predictors with the same number of columns as the \code{x} the
model was fitted with (an intercept column, if any, is added internally), or a numeric
vector for a single-predictor model.}

\item{interval}{if \code{TRUE}, also return the \code{level} prediction interval (a Student-t interval
around the mean response, mirroring \code{PredictionIntervals}). Default \code{FALSE}.}

\item{level}{prediction interval level, between 0 and 1. Default \code{0.90}, matching
\code{PredictionIntervals}'s own \code{alpha = 0.1} default.}

\item{...}{unused; present for generic consistency.}
}
\value{
with \code{interval = FALSE}, a numeric vector of predicted values, one per row of
\code{newdata}. With \code{interval = TRUE}, a matrix with columns \code{lower}, \code{upper}, \code{mean}.
}
\description{
Mirrors the C# \code{LinearRegression.Predict}/\code{PredictionIntervals} methods.
}
\examples{
x <- cbind(c(1, 2, 3, 4, 5), c(2, 1, 4, 3, 5))
y <- c(3.1, 4.2, 8.1, 9.2, 13.0)
fit <- linear_regression(x, y)
predict(fit, cbind(1, 2))
}
\seealso{
\code{\link[=linear_regression]{linear_regression()}}.
}
