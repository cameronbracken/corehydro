% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/timeseries.R
\name{ts_clip}
\alias{ts_clip}
\alias{ts_shift}
\alias{ts_convert_interval}
\title{Clip, shift and re-interval a time series}
\usage{
ts_clip(ts, start, end)

ts_shift(ts, by = c("day", "month", "year", "start"), amount = 0, start = NULL)

ts_convert_interval(ts, to, average = TRUE)
}
\arguments{
\item{ts}{a \code{corehydro_ts}.}

\item{start, end}{the new bounds, for \code{ts_clip()}.}

\item{by}{\code{"day"}, \code{"month"}, \code{"year"} or \code{"start"}.}

\item{amount}{the number of days, months or years to shift by.}

\item{to}{the target interval, one of \code{\link[=ts_interval_names]{ts_interval_names()}}.}

\item{average}{average across each block (the default) rather than summing.}
}
\value{
a \code{corehydro_ts}.
}
\description{
Clip, shift and re-interval a time series
}
\details{
\code{ts_clip()} keeps the ordinates inside \verb{[start, end]}; both bounds must lie inside the series'
own span. \code{ts_shift()} moves every date by a whole number of days, months or years, or -- with
\code{by = "start"} -- re-anchors the series at a new start date and re-walks the rest at its
interval (on an \code{"irregular"} series only the first ordinate moves, since there is no interval
to walk). \code{ts_convert_interval()} resamples to another interval, interpolating to a finer one
and block-averaging (or block-summing, with \code{average = FALSE}) to a coarser one; month,
quarter, year and irregular intervals have no fixed number of hours, so converting to or from
one raises.
}
\examples{
ts <- time_series(as.Date("2000-01-01") + 0:9, 1:10)
ts_clip(ts, as.Date("2000-01-03"), as.Date("2000-01-06"))
}
