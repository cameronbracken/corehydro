% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toolbox.R
\name{histogram}
\alias{histogram}
\title{Bin a sample into a histogram}
\usage{
histogram(x, bins = NULL)
}
\arguments{
\item{x}{numeric vector of observations, at least one element.}

\item{bins}{optional positive number of bins. \code{NULL} (the default) uses the Rice rule.}
}
\value{
a data frame with columns \code{lower}, \code{upper}, \code{midpoint}, and \code{frequency}, carrying the
histogram's \code{mean}, \code{median}, \code{mode}, \code{sd}, and \code{bin_width} as attributes.
}
\description{
Mirrors the C# \code{Histogram} class of the Numerics library. With \code{bins = NULL} the bin count
follows the Rice rule, \code{ceiling(2 * n^(1/3)) + 1}, exactly as the C# data constructor does.
}
\examples{
h <- histogram(c(1, 2, 2.5, 3, 3.5, 4, 5, 7, 8, 9))
h
attr(h, "mode")
}
