% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/copula.R
\name{copula}
\alias{copula}
\title{Construct a bivariate copula}
\usage{
copula(family, theta, df = NULL, margin_x = NULL, margin_y = NULL)
}
\arguments{
\item{family}{one of \code{\link[=copula_names]{copula_names()}}.}

\item{theta}{the dependence parameter.}

\item{df}{degrees of freedom, required for \code{"StudentT"} and ignored otherwise.}

\item{margin_x, margin_y}{optional \code{corehydro_dist} marginals (see Details).}
}
\value{
a \code{corehydro_copula}.
}
\description{
Build a copula from a family name and its dependence parameter(s). Mirrors the C#
\code{BivariateCopula} hierarchy of the Numerics library (\code{ClaytonCopula}, \code{GumbelCopula}, ...).
}
\details{
\code{margin_x} and \code{margin_y} are optional and are treated differently depending on their type,
which matters for \code{\link[=copula_random]{copula_random()}} (which needs marginals to draw on the data scale) and for
the IFM/full log-likelihoods:
\itemize{
\item a \code{corehydro_dist} (e.g. \code{distribution("Normal", c(0, 1))}) attaches that distribution
exactly as given, with no re-fitting.
\item this constructor does not accept a bare family-name string for the marginals; use
\code{\link[=copula_fit]{copula_fit()}} to have a named marginal MLE-fitted to a sample.
}
}
\examples{
copula_pdf(copula("Clayton", theta = 2), 0.3, 0.7)
}
\seealso{
\code{\link[=copula_fit]{copula_fit()}} to estimate a copula (and optionally its marginals) from data.
}
