% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toolbox.R
\name{stratify}
\alias{stratify}
\title{Stratify an axis into equal-width bins}
\usage{
stratify(lower, upper, bins, logarithmic = FALSE, probability = FALSE)
}
\arguments{
\item{lower, upper}{bounds of the axis to stratify. \code{lower} must be less than \code{upper}.}

\item{bins}{number of bins, greater than 1.}

\item{logarithmic}{stratify on a log10 scale. Default \code{FALSE}.}

\item{probability}{mark the axis as a probability axis; kept only for parity with the C#
constructor argument -- it always yields zero bins (see Details). Default \code{FALSE}.}
}
\value{
a data frame with columns \code{lower}, \code{upper}, \code{midpoint}, \code{weight}, one row per bin.
}
\description{
Mirrors the C# \code{Stratify.XValues(StratificationOptions, isLogarithmic)}: splits
\verb{[lower, upper]} into \code{bins} equal-width strata (equal-width in log10 space when
\code{logarithmic = TRUE}), each carrying a weight defaulting to its own width. \code{probability = TRUE} always returns zero rows, matching \code{Stratify.XValues}'s own early return for
probability-space options -- the ported header exposes only the \code{StratificationOptions}
overload used by the BestFit estimators' profile-likelihood grids, not the
probability-stratification methods (\code{Probabilities}, \code{XToProbability}, ...), which are out of
scope (see \code{stratify.hpp}'s file header).
}
\details{
\code{lower}, \code{upper}, and \code{bins} are validated rather than passed through to the C++ layer, which
would otherwise silently return zero rows for \code{lower >= upper} (and for \code{bins < 2}) instead of
raising an error.
}
\examples{
stratify(0, 1, bins = 4)
}
