% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toolbox.R
\name{gauss_jordan}
\alias{gauss_jordan}
\title{Gauss-Jordan elimination}
\usage{
gauss_jordan(a, b = NULL)
}
\arguments{
\item{a}{a square numeric matrix, N x N.}

\item{b}{a numeric matrix with N rows (the right-hand sides). Omit for the inverse alone,
in which case \code{solution} is an \verb{N x 0} matrix.}
}
\value{
a list with elements \code{inverse} (\code{a}'s inverse, N x N) and \code{solution} (\verb{N x ncol(b)}).
}
\description{
Mirrors the C# \verb{GaussJordanElimination::Solve(ref Matrix A, ref Matrix B)}: one full-pivot
Gauss-Jordan reduction that produces \code{a}'s inverse and, when \code{b} is supplied, the solution
set of \code{a \%*\% x = b}. Unlike the C# \verb{ref, ref} in-place API, \code{a} and \code{b} are never mutated
in R -- both results come back as new matrices.
}
\examples{
a <- matrix(c(1, 3, 3, 1, 4, 3, 1, 3, 4), nrow = 3, byrow = TRUE)
gauss_jordan(a)$inverse
}
