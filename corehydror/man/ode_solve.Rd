% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/callback.R
\name{ode_solve}
\alias{ode_solve}
\title{Solve a user-written ordinary differential equation}
\usage{
ode_solve(
  f,
  initial_value,
  start_time,
  end_time = NULL,
  time_steps = NULL,
  dt = NULL,
  dt_min = NULL,
  method = c("rk4", "rk2", "rkf", "cash_karp"),
  tolerance = NULL
)
}
\arguments{
\item{f}{a function taking two numbers (\code{t}, \code{y}) and returning \code{dy/dt}, one number.}

\item{initial_value}{the value of \code{y} at \code{start_time}.}

\item{start_time}{the time to start integrating from.}

\item{end_time, time_steps}{the end time and the number of equally-spaced points between
\code{start_time} and \code{end_time} (inclusive of both ends). Required together for \code{method = "rk2"}; for \code{method = "rk4"}, supplying them selects this array form over the single-step
form (see \code{dt} below) -- their PRESENCE, not a separate flag.}

\item{dt}{the step size. For \code{method = "rk4"} without \code{end_time}/\code{time_steps}, the single step
to advance by (the ODE is solved once, at \code{start_time + dt}). For \code{method = "rkf"}/
\code{"cash_karp"}, the maximum internal step size, required together with \code{dt_min}.}

\item{dt_min}{the minimum internal step size for \code{method = "rkf"}/\code{"cash_karp"}, required
together with \code{dt}.}

\item{method}{one of \code{"rk4"} (the default), \code{"rk2"}, \code{"rkf"}, or \code{"cash_karp"}.}

\item{tolerance}{the absolute error tolerance for \code{method = "rkf"}/\code{"cash_karp"} alone. \code{NULL},
the default, leaves the ported routine's own default (1e-3) in force.}
}
\value{
for \code{method = "rk2"}, or \code{"rk4"} with \code{end_time}/\code{time_steps}: a numeric vector of
length \code{time_steps}, the solution at each grid point (\code{y[1]} is \code{initial_value}). For every
other case: a single number, the solution at \code{start_time + dt} (\code{"rk4"}'s single-step form)
or at \code{start_time + dt} under the adaptive step control (\code{"rkf"}/\code{"cash_karp"}).
}
\description{
Solves \code{dy/dt = f(t, y)} forward from \code{start_time} with a ported Numerics Runge-Kutta method
(P2 "math extras"): fixed-step second- or fourth-order Runge-Kutta over an equally-spaced grid
(\code{method = "rk2"}/\code{"rk4"} with \code{end_time}/\code{time_steps}), the fourth-order method's single-step
form (\code{method = "rk4"} with \code{dt} alone), or one of the two adaptive-step-size methods,
Runge-Kutta-Fehlberg or Runge-Kutta-Cash-Karp (\code{method = "rkf"}/\code{"cash_karp"} with
\code{dt}/\code{dt_min}).
}
\examples{
# dy/dt = y, y(0) = 1: the exact solution is y(t) = exp(t).
y <- ode_solve(function(t, y) y, initial_value = 1, start_time = 0, end_time = 1,
               time_steps = 100)
y[100]
}
