% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{analysis_data_statistics}
\alias{analysis_data_statistics}
\title{Summary statistics for an observation record}
\usage{
analysis_data_statistics(data, all_data = FALSE, standardized = FALSE)
}
\arguments{
\item{data}{a numeric vector of observations, or a \code{corehydro_data} object from
\code{\link[=analysis_data]{analysis_data()}} (only its exact series is read).}

\item{all_data}{logical; use \code{SummaryStatisticsAllData} (see Details) instead of the default
exact-series-only statistics.}

\item{standardized}{logical; also run \code{SetStandardizedValues()} (after computing plotting
positions) and return the exact series' standardized values and standardized log10 values,
parallel to \code{\link[=analysis_data_summary]{analysis_data_summary()}}'s \code{value}.}
}
\value{
A named list. \code{value} holds the twenty summary statistics, named by
\code{c("Record Length", "Events Per Index (lambda)", "Low Outliers", "Minimum", "Maximum", "Mean", "Std Dev", "Skewness", "Kurtosis", "Mean (of log)", "Std Dev (of log)", "Skewness (of log)", "Kurtosis (of log)", "1\%", "5\%", "25\%", "50\%", "75\%", "95\%", "99\%")}. With
\code{standardized = TRUE}, \code{standardized_value} and \code{standardized_log10_value} are added: numeric
vectors parallel to the exact series, in series order. (The second name's parenthetical is the
Greek letter lambda, spelled out here rather than embedded literally so the PDF manual
renders; the actual returned name carries the Greek character, matching the C# source.)
}
\description{
Runs the \code{DataFrame} summary-statistics facades over the exact series of an \code{\link[=analysis_data]{analysis_data()}}
frame (or a plain numeric vector): record length, low-outlier count, min/max, the first four
product moments in real space and log10 space, and seven percentiles.
}
\details{
By default (\code{all_data = FALSE}) the statistics are computed directly from the exact series
(\code{SummaryStatisticsExactDataOnly}). With \code{all_data = TRUE}, they instead come from a
nonparametric distribution fit through the Hirsch-Stedinger plotting positions of the combined
exact/interval/uncertain record (\code{SummaryStatisticsAllData}) -- the same fit
\code{\link[=analysis_data_summary]{analysis_data_summary()}} uses -- so \code{"Record Length"} counts the full record (censored series
included) rather than the exact series alone, and the two methods can disagree even on a frame
with no censored data because their percentile and moment estimators differ. Both report \code{NaN}
for every value when the exact series has fewer than 10 points.
}
\examples{
peaks <- c(12500, 15300, 8900, 22100, 18700, 14200, 9800, 28500, 17400, 11600)
s <- analysis_data_statistics(peaks)
s$value[["Mean"]]
}
\seealso{
\code{\link[=analysis_data_hypothesis_test]{analysis_data_hypothesis_test()}} for the hypothesis-test facades,
\code{\link[=analysis_data_summary]{analysis_data_summary()}} for plotting positions and record diagnostics.
}
