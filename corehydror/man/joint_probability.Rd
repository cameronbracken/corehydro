% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toolbox.R
\name{joint_probability}
\alias{joint_probability}
\title{Joint probability of multiple events}
\usage{
joint_probability(
  p,
  dependency = c("independent", "positive", "negative", "correlation"),
  indicators = NULL,
  correlation = NULL
)
}
\arguments{
\item{p}{numeric vector of marginal probabilities.}

\item{dependency}{one of \code{"independent"} (default), \code{"positive"}, \code{"negative"},
\code{"correlation"}. \code{"correlation"} requires both \code{indicators} and \code{correlation} -- the
underlying C# method itself returns \code{NaN} for that combination rather than raising an
error, but this wrapper rejects it up front and names the missing argument(s), rather than
handing back a silent \code{NaN}.}

\item{indicators}{optional 0/1 vector, the same length as \code{p}.}

\item{correlation}{optional \code{length(p)} by \code{length(p)} correlation matrix; requires
\code{indicators}.}
}
\value{
a single numeric probability.
}
\description{
Mirrors the C# \code{Probability.JointProbability}. The plain form dispatches on a dependency
assumption alone (\code{"independent"} multiplies, \code{"positive"} takes the minimum, \code{"negative"}
clamps the excess of the sum over 1). Passing \code{indicators} (a 0/1 flag per component,
selecting which components participate) switches to the indicator-aware form; passing
\code{correlation} too routes \code{dependency = "correlation"} through Haden Smith's modification of
Pandey's Product-of-Conditional-Marginals method (HPCM).
}
\examples{
joint_probability(c(0.5, 0.5))
joint_probability(c(0.5, 0.5), dependency = "positive")
}
