% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/timeseries.R
\name{ts_statistics}
\alias{ts_statistics}
\alias{ts_hypothesis_test}
\alias{ts_percentiles}
\alias{ts_duration}
\title{Summary statistics of a time series}
\usage{
ts_statistics(ts)

ts_hypothesis_test(ts, split_location = NULL)

ts_percentiles(ts, probabilities = c(0.05, 0.25, 0.5, 0.75, 0.95))

ts_duration(ts)
}
\arguments{
\item{ts}{a \code{corehydro_ts}.}

\item{split_location}{the 1-BASED position in the observed values to split at (Python's
\code{hypothesis_test()} takes the 0-based position, each language following its own
convention). \code{NULL}, the default, splits the record in half.}

\item{probabilities}{the probabilities to report, in \verb{[0, 1]}.}
}
\value{
a named numeric vector, or for \code{ts_duration()} a data frame of \code{percent} and \code{value}.
}
\description{
Summary statistics of a time series
}
\details{
\code{ts_statistics()} returns the library's fifteen-entry summary (record length, missing count,
minimum, maximum, the four product moments and seven percentiles). \code{ts_hypothesis_test()}
returns the seven-test battery the container carries: Jarque-Bera for normality, Ljung-Box and
Wald-Wolfowitz for independence, Mann-Whitney and Mann-Kendall for homogeneity, and a t-test and
F-test comparing the two halves of the record. Note this is NOT the ten-test battery
\code{\link[=analysis_data_hypothesis_test]{analysis_data_hypothesis_test()}} runs on a flood-frequency data frame -- they are different
methods with different splits.
}
\examples{
ts <- time_series(as.Date("2000-01-01") + 0:11, c(22, 16, 33, 5, 12, 36, 48, 10, 18, 15, 22, 13))
ts_statistics(ts)
}
