% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/timeseries.R
\name{ts_replace_missing}
\alias{ts_replace_missing}
\alias{ts_interpolate_missing}
\alias{ts_fill_missing_dates}
\title{Handle missing values}
\usage{
ts_replace_missing(ts, value, indexes = NULL)

ts_interpolate_missing(ts, max_missing = 1, indexes = NULL)

ts_fill_missing_dates(ts, start, end, value = NA)
}
\arguments{
\item{ts}{a \code{corehydro_ts}.}

\item{value}{the replacement (or fill) value. \code{ts_fill_missing_dates()} defaults to \code{NA},
inserting the absent ordinates as missing rather than as zeros.}

\item{indexes}{optional 1-based ordinate positions to restrict the operation to.}

\item{max_missing}{the longest run of missing values to interpolate across.}

\item{start, end}{the date range to fill over.}
}
\value{
a \code{corehydro_ts}.
}
\description{
Handle missing values
}
\details{
\code{ts_replace_missing()} sets every missing value to \code{value}. \code{ts_interpolate_missing()} fills a
run of missing values by linear interpolation between its neighbours, but only when the run is
no longer than \code{max_missing}; a run reaching the END of the series is EXTRAPOLATED from the two
preceding ordinates instead. Both interpolate in date space, so an irregular spacing is
honoured. \code{ts_fill_missing_dates()} is the other half of the problem: it inserts the ordinates a
regular series is missing entirely, over the requested date range.
}
\examples{
ts <- time_series(as.Date("2000-01-01") + 0:4, c(3, NA, 4, 1, 5))
ts_interpolate_missing(ts, max_missing = 1)
}
