% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{model_ar}
\alias{model_ar}
\title{Autoregressive AR(p) model}
\usage{
model_ar(
  data,
  p = 1L,
  include_intercept = TRUE,
  transform = NULL,
  training_time_steps = NULL,
  parameters = NULL,
  parameter_values = NULL,
  use_default_flat_priors = NULL
)
}
\arguments{
\item{data}{numeric vector of the observed series, in sequence order.}

\item{p}{autoregressive order.}

\item{include_intercept}{logical; include an intercept term.}

\item{transform}{optional variance-stabilizing transform: \code{"None"}, \code{"Logarithmic"},
\code{"BoxCox"}, or \code{"YeoJohnson"}.}

\item{training_time_steps}{number of leading steps used for calibration, the rest held
back for validation. The model default is \code{max(30, floor(0.8 * n))}, which exceeds the
series length for any series shorter than 30 and then fails \code{\link[=model_validate]{model_validate()}}; set it
explicitly for a short series.}

\item{parameters}{optional \code{\link[=model_parameter]{model_parameter()}} object or list of them, setting bounds, fixed
flags, priors, or starting values.}

\item{parameter_values}{optional numeric vector of all parameter values, applied last.}

\item{use_default_flat_priors}{logical; set \code{FALSE} alongside a custom prior.}
}
\value{
An object of class \code{corehydro_model}.
}
\description{
Autoregressive AR(p) model
}
\examples{
x <- c(10.2, 11.5, 9.8, 12.1, 13.4, 11.9, 10.6, 12.8, 14.0, 13.1, 11.7, 12.5)
model_ar(x, p = 1)
}
\seealso{
\code{\link[=ar_analysis]{ar_analysis()}}.
}
