% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toolbox.R
\name{shortest_path}
\alias{shortest_path}
\title{Shortest paths through a network}
\usage{
shortest_path(
  from,
  to,
  weight,
  destinations,
  edge_index = NULL,
  node_count = NULL
)
}
\arguments{
\item{from, to, weight}{numeric vectors of the same length, one element per directed edge:
the start node index, the end node index, and the cost of traversing the edge. \code{from} and
\code{to} must be whole, non-negative numbers.}

\item{destinations}{a numeric vector of one or more destination node indices. With several
destinations, each node keeps whichever destination it reaches most cheaply.}

\item{edge_index}{an optional numeric vector the same length as \code{from}, labelling each edge
(typically an index into whatever the edges came from -- a river reach, a road segment).
Defaults to \code{0:(length(from) - 1)}. These labels are what the \code{edge_index} result column
reports, and they need not be distinct.}

\item{node_count}{an optional node count. Defaults to \code{max(from, to) + 1}; supply a larger
value to include isolated nodes carrying no edge, which then report \code{cost = Inf}. A value
below \code{max(from, to) + 1} is an error: the graph would not fit the routing table it asks for.}
}
\value{
a data frame with one row per node, in node-index order, and columns \code{next_node},
\code{edge_index} (both integer) and \code{cost} (numeric).
}
\description{
Mirrors the C# \code{Dijkstra.Solve} overloads: solves the cheapest route from EVERY node of a
directed, weighted graph to a set of destination nodes at once, running the search backwards
from the destinations. The answer is a routing table -- for each node, which neighbour to step
to, along which edge, and at what remaining cost.
}
\details{
Node indices are 0-based in both \code{corehydror} and \code{corehydropy}, matching the C# result table
the two packages share; a graph with \code{n} nodes uses indices \code{0} to \code{n - 1}. Unreachable nodes
carry \code{cost = Inf} with \code{next_node = -1} and \code{edge_index = -1}, and a destination node carries
\code{cost = 0} with \code{next_node} equal to its own index.

Costs accumulate in single precision, because the ported solver does (C# declares
\verb{float Weight} and its own tests assert the table by exact \code{float} equality). Fractional
weights therefore round to \code{float} before they are summed.
}
\examples{
# 0 -> 1 -> 2, plus a disconnected node 3
shortest_path(
  from = c(0, 1),
  to = c(1, 2),
  weight = c(1, 1),
  destinations = 2,
  node_count = 4
)
}
