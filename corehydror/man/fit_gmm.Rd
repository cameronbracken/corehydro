% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit_gmm}
\alias{fit_gmm}
\title{Generalized method of moments fit (Bulletin 17C)}
\usage{
fit_gmm(
  model,
  optimizer = "BFGS",
  strategy = "Iterative",
  max_gmm_iterations = 0L
)
}
\arguments{
\item{model}{a \code{\link[=model_bulletin17c]{model_bulletin17c()}} object.}

\item{optimizer}{one of \code{"BFGS"} (default, matching \code{GeneralizedMethodOfMoments}'s own class
default), \code{"NelderMead"}, \code{"Brent"}, \code{"Powell"}, \code{"DifferentialEvolution"},
\code{"MultilevelSingleLinkage"}.}

\item{strategy}{GMM estimation strategy: \code{"Iterative"} (default), \code{"OneStep"}, or \code{"TwoStep"}.}

\item{max_gmm_iterations}{maximum number of GMM iterations; \code{0} (default) keeps the estimator's
own default cap.}
}
\value{
An object of class \code{corehydro_fit} with \code{method == "GMM"}. Method of moments computes
no likelihood surface, so \verb{$log_likelihood}, \verb{$aic} and \verb{$bic} are \code{NA}; \verb{$nobs} is the
record length the estimator fitted against. Bulletin 17C is always
just-identified (as many moment conditions as parameters), so \verb{$j_stat_pval} is structurally
\code{NA} -- there is no over-identified case to report a p-value for (see
\code{docs/upstream-csharp-issues.md}). \verb{$gmm_iterations}, \verb{$converged_within_tolerance}, and
\verb{$optimizer_fallback_count} carry the estimator's own bookkeeping. See
\code{\link[=quantile_variance]{quantile_variance()}} for the delta-method variance of a fitted quantile, and
\code{\link[=fit_diagnostics]{fit_diagnostics()}} for leverage/influence diagnostics off a GMM fit.
}
\description{
Fit a \code{\link[=model_bulletin17c]{model_bulletin17c()}} model by the generalized method of moments and return a fit object
carrying the parameter estimates, the sandwich covariance, and the J-statistic
overidentification test. Wraps the shared C++ \code{GeneralizedMethodOfMoments} ported from
USACE-RMC RMC.BestFit -- the same estimator \code{\link[=bulletin17c_analysis]{bulletin17c_analysis()}} builds on. \code{IGMMModel}
(the interface GMM fits against) has exactly one implementation, \code{Bulletin17CDistribution}, so
\code{fit_gmm()} takes only a \code{model_bulletin17c()} model; unlike \code{\link[=fit_mle]{fit_mle()}}/\code{\link[=fit_map]{fit_map()}} there is no
plain-vector-plus-\code{distribution} convenience path.
}
\examples{
peaks <- c(12500, 15300, 8900, 22100, 18700, 14200, 9800, 28500, 17400, 11600)
f <- fit_gmm(model_bulletin17c(peaks))
f$parameters
quantile_variance(f, 0.01)
}
\seealso{
\code{\link[=fit_mle]{fit_mle()}}, \code{\link[=fit_map]{fit_map()}}, \code{\link[=fit_bayesian]{fit_bayesian()}}, \code{\link[=fit_diagnostics]{fit_diagnostics()}},
\code{\link[=quantile_variance]{quantile_variance()}}, \code{\link[=bulletin17c_analysis]{bulletin17c_analysis()}}.
}
