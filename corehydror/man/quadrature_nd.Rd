% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/callback.R
\name{quadrature_nd}
\alias{quadrature_nd}
\title{Integrate a user-written function over a multidimensional box}
\usage{
quadrature_nd(
  f,
  min,
  max,
  method = c("monte_carlo", "miser", "vegas"),
  seed = NULL,
  use_sobol = TRUE,
  max_function_evaluations = NULL,
  min_iterations = NULL,
  max_iterations = NULL,
  relative_tolerance = NULL,
  fraction = NULL,
  min_subregion_points = NULL,
  min_bisections = NULL,
  dither = NULL,
  independent_evaluations = NULL,
  function_calls = NULL,
  alpha = NULL,
  number_of_bins = NULL,
  tail_focus_parameter = NULL,
  initialize = NULL,
  check_convergence = NULL,
  target_probability = NULL
)
}
\arguments{
\item{f}{a function taking a numeric vector and returning one number (\code{method = "monte_carlo"}/\code{"miser"}), or a function taking a numeric vector and a number (the sample
weight) and returning one number (\code{method = "vegas"}).}

\item{min, max}{numeric vectors of the same length giving the per-dimension lower and upper
bounds; their common length is the number of dimensions. Every \code{max} entry must be above the
matching \code{min} entry.}

\item{method}{one of \code{"monte_carlo"} (the default), \code{"miser"}, or \code{"vegas"}.}

\item{seed}{an integer seed for the class's random number generator. \code{NULL}, the default,
leaves the ported class's own clock-seeded default in force -- so a \code{NULL} \code{"miser"}/\code{"vegas"}
run under Sobol sampling is still reproducible (the Sobol sequence is deterministic), but a
\code{NULL} \code{"monte_carlo"} run, or a \code{"miser"}/\code{"vegas"} run with \code{use_sobol = FALSE}, is not. See
the Details.}

\item{use_sobol}{whether to draw sample points from a Sobol low-discrepancy sequence rather
than the (possibly seeded) generator. Default \code{TRUE}. Only \code{"miser"} and \code{"vegas"} read this;
see the Details on why \code{"monte_carlo"} does not.}

\item{max_function_evaluations}{the cap on evaluations of \code{f}. \code{NULL}, the default, leaves the
ported class's own default in force. Applies to \code{"monte_carlo"} and \code{"miser"} alone --
\code{"miser"}'s own recursion is bounded directly by it, where \code{"monte_carlo"}'s loop is bounded
by \code{max_iterations} instead (see below); supplying it for \code{method = "vegas"} raises an error.}

\item{min_iterations, max_iterations, relative_tolerance}{\code{method = "monte_carlo"} alone: the
floor on iterations before the convergence check is consulted, the ceiling on iterations
(\code{"monte_carlo"}'s real throttle, since \code{max_function_evaluations} is checked only after the
loop ends to choose the reported status), and the relative-error convergence threshold. \code{NULL},
the default, leaves the ported class's own defaults in force. Supplying any for another
\code{method} raises an error.}

\item{fraction, min_subregion_points, min_bisections, dither}{\code{method = "miser"} alone: the
fraction of remaining evaluations spent exploring variance at each stage, the minimum points
per terminal subregion, the minimum evaluations before a subregion is bisected further, and
the dither applied when the integrand's active region falls on a subdivision boundary. \code{NULL},
the default, leaves the ported class's own defaults in force. Supplying any for another
\code{method} raises an error.}

\item{independent_evaluations, function_calls, alpha, number_of_bins, tail_focus_parameter, }{initialize,check_convergence,target_probability \code{method = "vegas"} alone. \code{independent_evaluations}
and \code{function_calls} bound the run (their product is the maximum total evaluations);
\code{alpha} is the grid-refinement damping exponent; \code{number_of_bins} the stratification bin
count; \code{tail_focus_parameter} the Power Transform exponent (1.0, the default, is standard
uniform sampling); \code{initialize} selects a cold start (0, the default), inheriting the grid
alone (1), or inheriting the grid and its answers (2); \code{check_convergence} whether to exit
early on convergence. \code{target_probability}, if supplied, calls the ported
\code{configure_for_rare_events()} helper -- applied AFTER every other option, so it may override
\code{number_of_bins}/\code{alpha}/\code{tail_focus_parameter}, exactly as the C# helper does. \code{NULL}, the
default, leaves the ported class's own defaults in force. Supplying any for another \code{method}
raises an error.}
}
\value{
the integral, a single number, carrying \code{status} (\code{"Success"},
\code{"MaximumFunctionEvaluationsReached"}, \code{"Failure"}, or \code{"None"}), \code{function_evaluations}, and
\code{standard_error} as attributes -- and, for \code{method = "vegas"} alone, \code{chi_squared}, the
Chi-Squared statistic (an approximate diagnostic for the run's own internal consistency across
its independent evaluations). An upstream quirk, verified against the real C# source rather
than assumed: \code{method = "miser"} always reports \code{status} \code{"None"} on success -- unlike
\code{"monte_carlo"} and \code{"vegas"}, the ported \code{Miser::integrate()} (faithfully mirroring C#'s
\code{Miser.Integrate()}) never assigns a success status, only ever writing \code{"Failure"} from its
catch block.
}
\description{
Computes the definite integral of \code{f} over the hyper-rectangle \verb{[min, max]} (P2 "math extras")
with one of three ported stochastic multidimensional integrators: plain Monte Carlo (\code{method = "monte_carlo"}, the default), Miser (recursive stratified-sampling Monte Carlo, Press et al.
"Numerical Recipes" Sec. 7.9), or Vegas (Lepage's adaptive importance sampling, Sec. 7.8, with
an optional Power Transform for rare tail-event sampling). \code{min}/\code{max} give both the
per-dimension bounds and, via their length, the number of dimensions.
}
\details{
\code{method = "vegas"} takes \code{f(x, weight)} -- \code{x} the sample point and \code{weight} the importance
weight Vegas has already computed for it -- rather than \code{f(x)}, matching the upstream C# Vegas
constructor's own integrand shape; a weight-ignoring wrapper (\code{function(x, w) g(x)}) reproduces
an \code{f(x)}-only integrand under Vegas, exactly as the upstream unit tests wrap theirs.

The SAMPLE STREAM -- which points \code{f} is called at -- reproduces bit-for-bit against the same
run in \code{corehydropy}, EXCEPT that \code{seed} has no effect on \code{method = "monte_carlo"} or a
Sobol-sampled run of \code{"miser"}/\code{"vegas"} (the default, \code{use_sobol = TRUE}): Miser and Vegas draw
their sample points from a Sobol low-discrepancy sequence rather than the Mersenne Twister
\code{seed} seeds, and \code{MonteCarloIntegration}'s own \code{UseSobolSequence} flag is a documented DEAD
property upstream -- declared but never consulted by \code{Integrate()} -- so a \code{"monte_carlo"} run
always draws from the generator \code{seed} seeds. \code{use_sobol = FALSE} reroutes Miser/Vegas through
that same seeded generator instead. An HONEST LIMIT, measured rather than assumed: the
AGGREGATED numbers this function returns are not always bit-identical between R and Python
the way the sample stream is. \code{MonteCarloIntegration}/\code{Miser}/\code{Vegas} are ported CORE code, so
-- unlike the callback surface's own catalog tests, which the C++ side compiles with
\code{-ffp-contract=off} for exactly this reason -- they compile with whatever fused-multiply-add
behavior each package's own build flags happen to produce (R's \code{-O2} and corehydropy's CMake
default are not guaranteed to agree). \code{integral} itself reproduces to within a handful of ULP in
every case measured; \code{standard_error} and (for \code{"vegas"}) \code{chi_squared}, both built from a
near-cancelling subtraction (\code{avg2 - avg*avg}-shaped for Monte Carlo/Miser,
\code{sum_chi_squared - sum_weighted_results * result} for Vegas), amplify that ULP-level difference
and can disagree well past it. This is a property of the classes' own arithmetic, not a bug in
either binding, and it is the same reason \code{fixtures/callback/callback_cross_language.json}'s own
\code{quadrature_nd}/\code{quadrature_vegas} digest asserts only \code{function_evaluations} and \code{status} --
see that file's reference note for the measurements.
}
\examples{
quadrature_nd(function(x) if (x[1]^2 + x[2]^2 < 1) 1 else 0, min = c(-1, -1), max = c(1, 1),
              seed = 12345)
quadrature_nd(function(x, w) if (x[1]^2 + x[2]^2 < 1) 1 else 0, min = c(-1, -1), max = c(1, 1),
              method = "vegas")
}
