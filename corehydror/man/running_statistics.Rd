% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toolbox.R
\name{running_statistics}
\alias{running_statistics}
\title{Streaming summary statistics}
\usage{
running_statistics(x, state = NULL)
}
\arguments{
\item{x}{numeric vector, the next chunk of data.}

\item{state}{a \code{corehydro_running} object from a previous call, or \code{NULL} (the default) to
start a fresh accumulator.}
}
\value{
a \code{corehydro_running} list with \code{n}, \code{minimum}, \code{maximum}, \code{mean}, \code{variance}, \code{sd},
\code{cv}, \code{skewness}, \code{kurtosis}, and the four raw moments \code{m1} to \code{m4}.
}
\description{
Accumulates count, extremes, and the first four moments over one or more chunks of data,
mirroring the C# \code{RunningStatistics} class. The accumulator state travels in the return value,
so a chunked run holds no C++ state and the result serializes with \code{save()}.
}
\examples{
s <- running_statistics(c(1, 2, 3))
s <- running_statistics(c(4, 5, 6), state = s)
s$n
s$mean
}
