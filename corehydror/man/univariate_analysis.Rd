% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{univariate_analysis}
\alias{univariate_analysis}
\title{Bayesian univariate frequency analysis}
\usage{
univariate_analysis(
  data,
  distribution,
  sampler = "DEMCz",
  iterations = 3000L,
  output_length = 10000L,
  credible_level = 0.9,
  seed = 12345L,
  exceedance_probabilities = NULL,
  thinning_interval = -1L
)
}
\arguments{
\item{data}{numeric vector of observations, or a \code{\link[=model_univariate]{model_univariate()}} model. A model carries
its own family, so \code{distribution} is then ignored, and it can bring censored observations
(see \code{\link[=analysis_data]{analysis_data()}}), nonstationary trends (see \code{\link[=trend]{trend()}}), and parameter bounds or
priors (see \code{\link[=model_parameter]{model_parameter()}}).}

\item{distribution}{distribution family name (e.g. \code{"Normal"}, \code{"GeneralizedExtremeValue"},
\code{"LogPearsonTypeIII"}) -- any name the core distribution factory accepts. Ignored when
\code{data} is a model.}

\item{sampler}{MCMC sampler: \code{"DEMCz"} (default), \code{"DEMCzs"}, \code{"ARWMH"}, or \code{"NUTS"}.}

\item{iterations}{number of post-warmup MCMC iterations.}

\item{output_length}{number of posterior samples used to build the credible band.}

\item{credible_level}{credible-interval width (e.g. \code{0.90} for a 90\% band).}

\item{seed}{PRNG seed for the sampler (fixed for reproducibility).}

\item{exceedance_probabilities}{optional numeric vector of exceedance probabilities at which to
tabulate the curve; when \code{NULL}, the 25 standard default ordinates are used. Must be in
\strong{ascending} order (as the defaults are); an unsorted vector fails validation and the
analysis reports itself as not valid.}

\item{thinning_interval}{MCMC thinning interval; \code{-1} (default) keeps the sampler's own default.}
}
\value{
A named list: \code{parameters} (point-estimate distribution parameters), \code{mode_curve},
\code{mean_curve}, \code{lower_ci}, \code{upper_ci} (one value per exceedance ordinate), and the scalars
\code{aic}, \code{bic}, \code{dic}, \code{rmse}.
}
\description{
Fit a univariate distribution to a sample with a Bayesian MCMC analysis and return the
frequency (quantile) curve, the posterior mean and credible band, and goodness-of-fit
scalars. Wraps the shared C++ \code{UnivariateAnalysis} ported from USACE-RMC RMC.BestFit.
}
\details{
Because both the R and Python packages call the identical compiled core with a bit-exact
Mersenne Twister, a seeded call returns identical numbers in either language.

The MCMC warmup (burn-in) length is set automatically to \code{max(50, iterations / 2)}; it
is not a user parameter.
}
\examples{
peaks <- c(12500, 15300, 8900, 22100, 18700, 14200, 9800, 28500, 17400, 11600)
fit <- univariate_analysis(peaks, "Normal", sampler = "DEMCzs",
                           iterations = 100, output_length = 400, seed = 12345)
fit$parameters

# The same analysis over a censored record, through a model.
d <- analysis_data(
  exact = peaks,
  interval = data.frame(index = 10, lower = 30000, value = 35000, upper = 40000)
)
fit2 <- univariate_analysis(model_univariate("Normal", d), sampler = "DEMCzs",
                            iterations = 100, output_length = 400, seed = 12345)
fit2$parameters
}
