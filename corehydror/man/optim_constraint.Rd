% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optim.R
\name{optim_constraint}
\alias{optim_constraint}
\title{Declare one constraint for the augmented Lagrange optimizer}
\usage{
optim_constraint(f, value, type = c("eq", "le", "ge"), tolerance = 1e-08)
}
\arguments{
\item{f}{a function taking a numeric parameter vector and returning a single number -- the
left-hand side of the constraint.}

\item{value}{the number \code{f} is compared against.}

\item{type}{\code{"eq"} for \code{f(x) == value}, \code{"le"} for \code{f(x) <= value}, or \code{"ge"} for
\code{f(x) >= value}.}

\item{tolerance}{how far from \code{value} still counts as feasible, matching the upstream
\code{Constraint} class's own default.}
}
\value{
a \code{corehydro_constraint} list.
}
\description{
Pairs a constraint function with the value it is compared against and the comparison type.
Pass a list of these as \code{constraints} to \code{\link[=optim_minimize]{optim_minimize()}} with
\code{method = "augmented_lagrange"}.
}
\examples{
# x + y == 4
optim_constraint(function(p) p[1] + p[2], value = 4, type = "eq")
}
