% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toolbox.R
\name{polynomial_eval}
\alias{polynomial_eval}
\title{Evaluate a polynomial}
\usage{
polynomial_eval(
  coefficients,
  x,
  variant = c("standard", "reverse", "reverse_unit"),
  n = NULL
)
}
\arguments{
\item{coefficients}{a numeric vector of polynomial coefficients.}

\item{x}{a numeric vector, the points at which to evaluate.}

\item{variant}{one of \code{"standard"} (default), \code{"reverse"}, or \code{"reverse_unit"}.}

\item{n}{an optional integer redefining the polynomial's order to \code{n + 1}; only valid with
\code{variant = "reverse"}.}
}
\value{
a numeric vector the same length as \code{x}.
}
\description{
Mirrors the C# \code{Evaluate} class's three polynomial evaluators (Horner's method), vectorized
over \code{x} against one shared \code{coefficients} vector: \code{variant = "standard"} calls
\code{Evaluate.Polynomial} (coefficients in ASCENDING order, \code{coefficients[1]} the constant term);
\code{"reverse"} calls \code{Evaluate.PolynomialRev} (coefficients in DESCENDING order,
\code{coefficients[1]} the highest-order term), optionally truncated to order \code{n + 1} via \code{n};
\code{"reverse_unit"} calls \code{Evaluate.PolynomialRev_1} (DESCENDING order with an implicit leading
coefficient of 1).
}
\examples{
polynomial_eval(c(3, 5, 7), 4)
polynomial_eval(c(3, 5, 7), 4, variant = "reverse")
polynomial_eval(c(3, 5, 7), 4, variant = "reverse", n = 1)
}
