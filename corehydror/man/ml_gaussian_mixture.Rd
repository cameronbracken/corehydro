% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml.R
\name{ml_gaussian_mixture}
\alias{ml_gaussian_mixture}
\title{Gaussian mixture model}
\usage{
ml_gaussian_mixture(
  x,
  k,
  seed = NULL,
  kmeans_plus_plus = TRUE,
  max_iterations = 1000,
  tolerance = 1e-08
)
}
\arguments{
\item{x}{a numeric matrix or data frame with one row per observation, or a numeric vector.}

\item{k}{the number of mixture components.}

\item{seed}{integer PRNG seed; \code{NULL} (the default) uses the computer clock.}

\item{kmeans_plus_plus}{use k-means++ initialization for the starting fit. Default \code{TRUE}.}

\item{max_iterations}{the EM iteration cap. Default 1000.}

\item{tolerance}{the relative convergence tolerance on the log-likelihood. Default 1e-8.}
}
\value{
a list with \code{means} (\code{k} by \code{ncol(x)}), \code{sigmas} (a length-\code{k} list of
\code{ncol(x)} by \code{ncol(x)} covariance matrices), \code{weights}, \code{labels} (0-based),
\code{log_likelihood}, and \code{iterations}.
}
\description{
Mirrors the C# \code{GaussianMixtureModel} class: fits a mixture of \code{k} multivariate normals by
expectation-maximization, initialized from a k-means fit. It generalizes \code{\link[=ml_kmeans]{ml_kmeans()}} by
carrying a full covariance per component rather than only a centre.
}
\details{
\code{labels} are 0-BASED, as in \code{\link[=ml_kmeans]{ml_kmeans()}}. \code{log_likelihood} follows the library's own
definition, which OMITS the multivariate-normal normalizing constant
\code{-0.5 * ncol(x) * log(2 * pi)} per observation -- so it is short of the true mixture
log-likelihood by \code{nrow(x) * ncol(x) / 2 * log(2 * pi)}. That constant cancels when comparing
two fits of the same data (which is what the library uses it for), but do not feed this value
to an information criterion without adding it back. A run that hits \code{max_iterations} without
converging reports \code{log_likelihood} as 0.
}
\examples{
x <- c(1, 1.2, 0.9, 1.1, 1.05, 8, 8.3, 7.9, 8.1, 8.2, 1.0, 8.0)
fit <- ml_gaussian_mixture(x, k = 2, seed = 12345)
round(fit$weights, 3)
}
