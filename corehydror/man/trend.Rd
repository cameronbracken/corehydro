% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{trend}
\alias{trend}
\title{Attach a trend to a distribution parameter}
\usage{
trend(parameter, type, start_index = NULL, values = NULL)
}
\arguments{
\item{parameter}{the distribution parameter to trend, as a name (\code{"location"}) or a 1-based
position (\code{1}). Names are resolved against the family's parameter names; see
\code{\link[=dist_params]{dist_params()}}.}

\item{type}{the trend model: one of \code{"Constant"}, \code{"Linear"}, \code{"Quadratic"}, \code{"Cubic"},
\code{"Exponential"}, \code{"Logistic"}, \code{"Power"}, \code{"Reciprocal"}, \code{"Sinusoidal"}, \code{"StepFunction"},
or \code{"GeneralLinear"}.}

\item{start_index}{optional 0-based index the trend is anchored at; the data-driven default is
used when omitted.}

\item{values}{optional numeric vector of trend coefficients, length matching the trend's own
parameter count.}
}
\value{
An object of class \code{corehydro_trend}.
}
\description{
Describe a nonstationary trend on one parameter of a distribution, for
\code{\link[=model_univariate]{model_univariate()}}. Attaching any trend makes the model nonstationary: the trended
parameter is replaced in the parameter vector by the trend's own coefficients, so a linear
trend on the location turns one location parameter into an intercept and a slope.
}
\examples{
trend("location", "Linear")
trend(1, "Quadratic", values = c(100, 2.5, -0.01))
}
\seealso{
\code{\link[=model_univariate]{model_univariate()}}, \code{\link[=model_parameter]{model_parameter()}}.
}
