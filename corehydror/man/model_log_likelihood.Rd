% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{model_log_likelihood}
\alias{model_log_likelihood}
\title{Model log-likelihood}
\usage{
model_log_likelihood(model, params = NULL)
}
\arguments{
\item{model}{a \code{corehydro_model} from one of the \verb{model_*()} constructors.}

\item{params}{optional numeric vector of parameter values; the model's own current values are
used when omitted.}
}
\value{
A named list: \code{log_likelihood}, \code{data_log_likelihood}, \code{prior_log_likelihood}, and
\code{parameters} (the vector the model actually evaluated, which a mixture model rewrites in
place to normalize its weights).
}
\description{
Evaluate a model's log-likelihood, decomposed into its data and prior halves.
}
\examples{
peaks <- c(12500, 15300, 8900, 22100, 18700, 14200, 9800, 28500, 17400, 11600)
m <- model_univariate("Normal", peaks)
model_log_likelihood(m)
model_log_likelihood(m, c(16000, 6000))
}
