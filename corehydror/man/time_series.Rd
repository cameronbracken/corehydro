% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/timeseries.R
\name{time_series}
\alias{time_series}
\title{Create a time series}
\usage{
time_series(dates, values, interval = "one_day")
}
\arguments{
\item{dates}{the ordinate dates.}

\item{values}{the ordinate values, one per date.}

\item{interval}{one of \code{\link[=ts_interval_names]{ts_interval_names()}}. Default \code{"one_day"}.}
}
\value{
a \code{corehydro_ts} object.
}
\description{
Builds the object every \verb{ts_*()} verb takes: an ordered collection of (date, value) ordinates
on a stated time interval, mirroring the Numerics \code{TimeSeries} container.
}
\details{
\code{dates} may be \code{POSIXct}, \code{Date}, ISO 8601 character strings, or numeric seconds since
1970-01-01; they are carried as UTC \code{POSIXct} and returned that way. Missing observations are
\code{NA} (or \code{NaN}) in \code{values} and stay missing through every verb that says so.

The interval is not merely a label: it is what \code{ts_shift(by = "start")} walks, what
\code{ts_peaks_over_threshold()} measures its independence criterion in, and what
\code{ts_convert_interval()} converts from. \code{"irregular"} is the right choice for an event series
(annual maxima, peaks over threshold) whose spacing is not fixed.
}
\examples{
ts <- time_series(as.Date("2000-01-01") + 0:9, c(3, 1, 4, 1, 5, 9, 2, 6, 5, 3))
ts
}
\seealso{
\code{\link[=ts_block_series]{ts_block_series()}} for annual maxima, \code{\link[=ts_peaks_over_threshold]{ts_peaks_over_threshold()}} for a POT series,
\code{\link[=ts_statistics]{ts_statistics()}} for a summary.
}
