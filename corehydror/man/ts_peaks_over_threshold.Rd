% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/timeseries.R
\name{ts_peaks_over_threshold}
\alias{ts_peaks_over_threshold}
\title{Peaks over a threshold}
\usage{
ts_peaks_over_threshold(
  ts,
  threshold,
  min_steps_between_events = 1,
  smoothing = c("none", "moving_average", "moving_sum", "difference"),
  period = 1
)
}
\arguments{
\item{ts}{a \code{corehydro_ts}.}

\item{threshold}{the exceedance threshold.}

\item{min_steps_between_events}{the minimum number of time steps between independent events.
Default 1.}

\item{smoothing}{\code{"none"} (the default), \code{"moving_average"}, \code{"moving_sum"} or \code{"difference"},
applied before the extraction.}

\item{period}{the smoothing window. Default 1.}
}
\value{
a \code{corehydro_ts} on the \code{"irregular"} interval, one ordinate per event.
}
\description{
Extracts independent peak events above \code{threshold}, following the \code{clust} method of the R
\code{POT} package: the first exceedance opens a cluster, the first value back under the threshold
closes it unless the minimum spacing has not yet elapsed, and the next exceedance opens the
next cluster. Each cluster contributes its maximum.
}
\examples{
ts <- time_series(as.Date("2000-01-01") + 0:9, c(1, 8, 2, 1, 9, 7, 1, 2, 12, 1))
ts_peaks_over_threshold(ts, threshold = 5, min_steps_between_events = 2)
}
\seealso{
\code{\link[=ts_block_series]{ts_block_series()}} for the block-maxima alternative.
}
