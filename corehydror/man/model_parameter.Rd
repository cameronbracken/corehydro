% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{model_parameter}
\alias{model_parameter}
\title{Constrain or prime one model parameter}
\usage{
model_parameter(
  parameter,
  value = NULL,
  lower = NULL,
  upper = NULL,
  fixed = NULL,
  prior = NULL
)
}
\arguments{
\item{parameter}{the parameter to constrain, as a name (\code{"scale"}) or a 1-based position. In a
model carrying trends the parameter vector is wider than the distribution's, so a position
is required there.}

\item{value}{optional starting value. A \code{parameter_values} vector on the model wins over this.}

\item{lower, upper}{optional bounds used by the estimators and samplers.}

\item{fixed}{logical; hold the parameter at its value instead of estimating it.}

\item{prior}{optional \code{\link[=distribution]{distribution()}} object used as the parameter's prior.}
}
\value{
An object of class \code{corehydro_model_parameter}.
}
\description{
Set the bounds, fixed flag, prior distribution, or starting value of a single model parameter.
Pass the result to the \code{parameters} argument of any \verb{model_*()} constructor.
}
\details{
Supplying a prior replaces the model's default. Do that together with
\code{use_default_flat_priors = FALSE} on the model, so a later data assignment cannot overwrite
what you set.
}
\examples{
model_parameter("scale", lower = 0.5, upper = 40)
model_parameter("mean", prior = distribution("Normal", c(15000, 4000)))
}
\seealso{
\code{\link[=model_univariate]{model_univariate()}}, \code{\link[=trend]{trend()}}.
}
