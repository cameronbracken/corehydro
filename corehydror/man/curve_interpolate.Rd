% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toolbox.R
\name{curve_interpolate}
\alias{curve_interpolate}
\title{Interpolate a paired x-y curve}
\usage{
curve_interpolate(
  x,
  y,
  xout = NULL,
  yout = NULL,
  x_transform = c("none", "logarithmic", "normal_z"),
  y_transform = c("none", "logarithmic", "normal_z"),
  order_x = c("ascending", "descending", "none"),
  order_y = c("ascending", "descending", "none"),
  strict_x = TRUE,
  strict_y = TRUE
)
}
\arguments{
\item{x, y}{numeric vectors of equal length (at least two), the curve's ordinates.}

\item{xout}{numeric vector of x positions to interpolate y at.}

\item{yout}{numeric vector of y positions to interpolate x at.}

\item{x_transform, y_transform}{one of \code{"none"} (default), \code{"logarithmic"}, or \code{"normal_z"}.}

\item{order_x, order_y}{one of \code{"ascending"} (default), \code{"descending"}, or \code{"none"}.}

\item{strict_x, strict_y}{require x/y to strictly increase/decrease (per \code{order_x}/\code{order_y})
between consecutive ordinates. Default \code{TRUE}.}
}
\value{
a numeric vector, the same length as whichever of \code{xout}/\code{yout} was supplied.
}
\description{
Mirrors the C# \code{OrderedPairedData.GetYFromX}/\code{GetXFromY}: linear interpolation over a curve
that keeps itself sorted/validated against a caller-chosen monotonicity contract, with
optional per-axis transforms (log10 or the standard normal z-score) applied before/after
interpolation. Exactly one of \code{xout}/\code{yout} must be supplied.
}
\examples{
curve_interpolate(c(50, 100, 150, 200, 250), c(100, 200, 300, 400, 500), xout = 75)
}
