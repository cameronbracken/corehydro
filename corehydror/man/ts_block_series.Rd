% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/timeseries.R
\name{ts_block_series}
\alias{ts_block_series}
\alias{ts_water_year}
\alias{ts_calendar_year}
\title{Block series: annual maxima and other block extremes}
\usage{
ts_block_series(
  ts,
  window = c("water_year", "calendar_year", "custom_year", "quarter", "month"),
  block = c("maximum", "minimum", "average", "sum"),
  smoothing = c("none", "moving_average", "moving_sum", "difference"),
  period = 1,
  start_month = 10,
  end_month = 9
)

ts_water_year(
  ts,
  block = "maximum",
  smoothing = "none",
  period = 1,
  start_month = 10
)

ts_calendar_year(ts, block = "maximum", smoothing = "none", period = 1)
}
\arguments{
\item{ts}{a \code{corehydro_ts}.}

\item{window}{\code{"water_year"}, \code{"calendar_year"}, \code{"custom_year"}, \code{"quarter"} or \code{"month"}.}

\item{block}{the block function: \code{"maximum"} (the default), \code{"minimum"}, \code{"average"} or
\code{"sum"}.}

\item{smoothing}{\code{"none"} (the default), \code{"moving_average"}, \code{"moving_sum"} or \code{"difference"}.}

\item{period}{the smoothing window (or the difference lag). Default 1, which is a no-op for the
two moving windows.}

\item{start_month}{the month a water year or custom year begins. Default 10 (October).}

\item{end_month}{the month a custom year ends. Default 9 (September).}
}
\value{
a \code{corehydro_ts} on the \code{"irregular"} interval.
}
\description{
Reduces a series to one value per time block -- the classical first step of a flood-frequency
analysis. \code{ts_water_year()} and \code{ts_calendar_year()} are the two common cases by name.
}
\details{
The result is an IRREGULAR series carrying the date of the observation the block function
selected: for \code{"minimum"} and \code{"maximum"} that is the extreme observation's own date, and for
\code{"sum"} and \code{"average"} it is the block's last date.

A block whose values include a missing one is dropped by \code{"sum"} and \code{"average"} (the missing
value propagates) but kept by \code{"minimum"} and \code{"maximum"} (the comparison is false against a
missing value, so it is simply never selected). That is the library's behaviour and it is worth
knowing before reading an annual maximum series built from a gappy record.

\code{smoothing} applies BEFORE the block function, which is how an n-day average maximum is built:
\code{ts_water_year(ts, smoothing = "moving_average", period = 7)} is the annual maximum 7-day mean.
}
\examples{
dates <- seq(as.Date("2000-01-01"), by = "month", length.out = 36)
ts <- time_series(dates, c(5, 9, 3, 7, 2, 8, 4, 6, 1, 5, 7, 3,
                           6, 2, 8, 4, 9, 3, 7, 5, 2, 8, 4, 6,
                           3, 7, 5, 9, 2, 6, 4, 8, 1, 5, 3, 7))
ts_water_year(ts)
}
\seealso{
\code{\link[=ts_peaks_over_threshold]{ts_peaks_over_threshold()}} for the partial-duration alternative, \code{\link[=fit_mle]{fit_mle()}} for
fitting the result.
}
