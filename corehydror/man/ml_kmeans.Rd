% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml.R
\name{ml_kmeans}
\alias{ml_kmeans}
\title{k-means clustering}
\usage{
ml_kmeans(x, k, seed = NULL, kmeans_plus_plus = TRUE, max_iterations = 1000)
}
\arguments{
\item{x}{a numeric matrix or data frame with one row per observation, or a numeric vector for
a single feature.}

\item{k}{the number of clusters.}

\item{seed}{integer PRNG seed. \code{NULL} (the default) uses the computer clock, so the fit is not
reproducible; supply a seed for a reproducible fit.}

\item{kmeans_plus_plus}{use k-means++ initialization (the default) rather than a uniform draw.}

\item{max_iterations}{the iteration cap. Default 1000.}
}
\value{
a list with \code{means} (a \code{k} by \code{ncol(x)} matrix of cluster centroids), \code{labels} (a
0-based integer vector, one per row of \code{x}), and \code{iterations}.
}
\description{
Mirrors the C# \code{KMeans} class: partitions the rows of \code{x} into \code{k} clusters, each row
belonging to the cluster with the nearest centroid. Initialization is k-means++ by default.
}
\details{
Two behaviours inherited from upstream are worth knowing:
\itemize{
\item \code{labels} are 0-BASED in both R and Python, matching the library's own indexing (the same
choice \code{\link[=shortest_path]{shortest_path()}} made for node indices). Add 1 before using them to subset an R
object.
\item With \code{k = 1} the algorithm stops before its first update step, so the single reported
"cluster mean" is a randomly chosen observation rather than the mean of \code{x}. Use \code{mean()}
instead if that is what you want.
}
}
\examples{
x <- cbind(c(1, 1.2, 0.8, 8, 8.3, 7.9), c(2, 2.1, 1.9, 9, 9.2, 8.8))
fit <- ml_kmeans(x, k = 2, seed = 12345)
fit$means
}
\seealso{
\code{\link[=ml_gaussian_mixture]{ml_gaussian_mixture()}} for a covariance-aware generalization.
}
