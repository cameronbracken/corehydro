% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/callback.R
\name{mcmc_posterior}
\alias{mcmc_posterior}
\title{Sample your own posterior by MCMC}
\usage{
mcmc_posterior(
  log_likelihood,
  priors,
  sampler = "RWMH",
  proposal = NULL,
  gradient = NULL,
  iterations = NULL,
  warmup = NULL,
  chains = NULL,
  thinning = NULL,
  output_length = NULL,
  seed = 12345,
  initialize = "MAP"
)
}
\arguments{
\item{log_likelihood}{a function taking a numeric parameter vector and returning a single
number.}

\item{priors}{a list of \code{\link[=distribution]{distribution()}} objects, one per parameter, in the order
\code{log_likelihood} reads them. A single distribution is accepted for a one-parameter model.}

\item{sampler}{one of \code{"RWMH"} (the default), \code{"ARWMH"}, \code{"DEMCz"}, \code{"DEMCzs"}, \code{"HMC"},
\code{"NUTS"}, \code{"SNIS"}, or \code{"Gibbs"}. \code{"Gibbs"} requires \code{proposal}.}

\item{proposal}{the conditional proposal function \code{"Gibbs"} samples with, and the only sampler
that takes one. It is called as \code{proposal(parameters, rng)} and must return a numeric vector
as long as \code{priors}: the next state of the chain, which Gibbs accepts unconditionally.
Ordinarily that is a draw from the full conditional of the model. \code{rng} is a handle on the
generator this chain is running on -- draw from it with \code{\link[=rng_uniform]{rng_uniform()}} and \code{\link[=rng_integers]{rng_integers()}},
not with \code{\link[stats:runif]{stats::runif()}}, or the seeded run stops being reproducible.}

\item{gradient}{an analytic gradient of \code{log_likelihood} for \code{"HMC"} and \code{"NUTS"}, the only
samplers that take one. It is called as \code{gradient(parameters)} and must return a numeric
vector as long as \code{priors}. Left \code{NULL}, both samplers use the ported bound-aware
finite-difference gradient, which costs two extra \code{log_likelihood} calls per parameter per
leapfrog step; an analytic gradient is usually a large saving and is always more accurate.}

\item{iterations}{iterations per chain (sampler default if \code{NULL}). \code{"SNIS"} needs at least
10000 unless \code{output_length} is lowered too: its ported validation requires \code{iterations} to
be at least the output length, whose default is 10000.}

\item{warmup}{warm-up iterations discarded from each chain (sampler default if \code{NULL}). When
\code{iterations} is given and \code{warmup} is not, half of \code{iterations} is used, matching
\code{\link[=mcmc_sample]{mcmc_sample()}}. \code{"SNIS"} is the exception: its ported validation rejects any warm-up at all,
so none is derived for it.}

\item{chains}{number of chains (sampler default if \code{NULL}). \code{"DEMCz"} and \code{"DEMCzs"} require
at least three; \code{"SNIS"} draws independently and runs one.}

\item{thinning}{thinning interval (sampler default if \code{NULL}).}

\item{output_length}{total number of retained draws across all chains (sampler default, 10,000,
if \code{NULL}). The ported sampler collects \code{ceiling(output_length / chains)} draws per chain
after the iteration loop, so this is the second setting -- with \code{thinning} -- that multiplies
how many times your function is called. The ported floor is 100 and it is refused below that.}

\item{seed}{PRNG seed; \code{12345} is the C# default.}

\item{initialize}{chain initialization: \code{"MAP"} (from the posterior-mode estimate, the C#
default) or \code{"Randomize"} (draws from the priors).}
}
\value{
A list with the same fields \code{\link[=mcmc_sample]{mcmc_sample()}} returns: \code{parameters} (parameter names,
\code{p1}, \code{p2}, ... since your model names nothing), \code{chains} (a list of one
draws-by-parameters matrix per chain), \code{acceptance_rates}, \code{map}, \code{map_fitness},
\code{posterior_mean}, \code{posterior_sd}, \code{posterior_median}, \code{posterior_lower_ci},
\code{posterior_upper_ci}, \code{rhat}, and \code{ess}.
}
\description{
Runs any of the ported MCMC samplers against a log-likelihood you write and priors you
choose. This is the constructor the upstream C# library itself exposes,
\code{MCMCSampler(priorDistributions, logLikelihoodFunction)}: \code{\link[=mcmc_sample]{mcmc_sample()}} can only fit a
built-in distribution family under uniform priors spanning its parameter constraints, while
this function takes any model you can write down.
}
\details{
\code{log_likelihood} is called with one numeric vector, as long as \code{priors}, and must return a
single number. Following the upstream contract, it should return the log of the data
likelihood PLUS the log prior density; the \code{priors} list is used for the feasible parameter
bounds and for chain initialization, and is never added to your value behind your back. A
parameter outside its prior's support is rejected before your function sees it, so a flat
(uniform) prior needs no term of its own.
}
\section{Reproducing a run in Python}{

A seeded \code{\link[=mcmc_sample]{mcmc_sample()}} run is bit-identical between R and Python, because every arithmetic
operation happens in the shared C++ core. That guarantee is WEAKER here, and it is worth
stating plainly. The draws still come from the core's seeded Mersenne Twister, but the
log-density is your own R code, and R and Python do not guarantee identical rounding for the
same formula. MCMC amplifies a single differing bit far harder than an optimizer does: one
flipped accept-or-reject changes every state after it, so the two chains diverge outright
rather than drift apart slowly.

A seeded run reproduces across the two languages if and only if your function returns
bit-identical values. Arithmetic (\verb{+ - * /}) is IEEE-deterministic and does reproduce; \code{log},
\code{exp}, \code{gamma} and friends come from each platform's own math library and are not guaranteed
to. Note also that R's \code{sum()} accumulates in extended precision while Python's does not, so
an explicit loop or \code{Reduce()} is the portable spelling.
}

\section{Performance}{

Every evaluation calls back into R, and there are far more of them than \code{iterations} suggests:
the count is \code{(iterations + output_length / chains) * thinning * chains}, and the ported
defaults are 4 chains, a thinning interval of 20 and an output length of 10,000. So
\code{iterations = 10000} is a million crossings, not ten thousand.

That is affordable. All the figures here come from one machine, one session and one problem --
a 50-point Gaussian log-density at \code{iterations = 10000}, so exactly the million evaluations
above -- and \code{corehydropy}'s help page reports the same experiment, so the two can be read
side by side. The run took 5.0 seconds, against 1.3 seconds for the \code{\link[=mcmc_sample]{mcmc_sample()}} call on
the same data: the callback path is about four times the built-in one, and most of that is not
the boundary. The same closure called a million times directly from R takes 2.7 seconds, so
your own code is over half the total. The crossing itself is about 1.5 microseconds, measured
by replacing the log-density with \code{function(p) 0}, which brings the run to 1.7 seconds.

Two settings dominate the count and neither is obvious. \code{thinning} multiplies it, so
\code{thinning = 1} turned the same run into 0.23 seconds. And \code{initialize = "MAP"}, the C# default,
runs the DifferentialEvolution optimizer over your function before the first chain iteration;
\code{initialize = "Randomize"} skips the optimizer.

\code{"Gibbs"} is the sampler whose defaults surprise: the ported constructor sets one chain, no
thinning, and 100,000 iterations on top of a 10,000-draw output block, so an \code{iterations} you do
not set is 110,000 iterations of BOTH your log-likelihood and your proposal. Set \code{iterations}.
}

\section{Interrupting a long run}{

Ctrl-C returns control with an interrupt condition, but not instantly. The ported samplers have
no cancellation hook, so the chain runs to the end of its loop -- rejecting every remaining
point without calling your function again -- before the interrupt surfaces. The wait is
therefore set by how much of the run is left, not by the interrupt: measured on a
100,000-iteration chain interrupted one second in, 3.2 seconds from Ctrl-C to the prompt.
\code{corehydropy} behaves the same way and reports the same measurement at 200,000 iterations.
}

\examples{
\donttest{
set.seed(1)
x <- rnorm(50, mean = 5)
# A plain loop and `+ - * /` alone, the portable spelling described above.
ll <- function(p) {
  acc <- 0
  for (xi in x) acc <- acc + (xi - p[1]) * (xi - p[1])
  -0.5 * acc
}
fit <- mcmc_posterior(ll, list(distribution("Uniform", c(0, 10))),
                      iterations = 500, seed = 12345)
fit$posterior_mean
}
}
\seealso{
\code{\link[=mcmc_sample]{mcmc_sample()}} for a built-in family under constraint-based priors, which is faster
and bit-identical across languages.
}
