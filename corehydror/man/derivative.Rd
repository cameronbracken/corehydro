% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/callback.R
\name{derivative}
\alias{derivative}
\alias{gradient}
\alias{hessian}
\title{Differentiate a user-written function}
\usage{
derivative(f, x, step_size = -1)

gradient(f, x)

hessian(f, x)
}
\arguments{
\item{f}{for \code{derivative()}, a function taking one number and returning one number; for
\code{gradient()} and \code{hessian()}, a function taking a numeric vector and returning one number.}

\item{x}{the point to differentiate at: one number for \code{derivative()}, a numeric vector for
\code{gradient()} and \code{hessian()}.}

\item{step_size}{the finite-difference step for \code{derivative()}. The default, any value at or
below zero, selects the adaptive step \verb{eps^(1/2) * (1 + |x|)}.}
}
\value{
\code{derivative()} returns a single number, \code{gradient()} a numeric vector the length of
\code{x}, and \code{hessian()} a square symmetric matrix.
}
\description{
\code{derivative()} takes the first derivative of a single-variable function by central difference;
\code{gradient()} and \code{hessian()} take the gradient and the Hessian matrix of a function of a
parameter vector. All three are the ported Numerics \code{NumericalDerivative} routines.
}
\examples{
derivative(function(x) x^3, 2)
rosenbrock <- function(p) (1 - p[1])^2 + 100 * (p[2] - p[1]^2)^2
gradient(rosenbrock, c(1, 1))
hessian(rosenbrock, c(1, 1))
}
