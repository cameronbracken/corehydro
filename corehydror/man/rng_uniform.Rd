% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/callback.R
\name{rng_uniform}
\alias{rng_uniform}
\alias{rng_integers}
\title{Draw from the generator a callback is handed}
\usage{
rng_uniform(rng, n)

rng_integers(rng, n, min, max)
}
\arguments{
\item{rng}{the handle your callback was given. It cannot be created any other way.}

\item{n}{how many values to draw; a single positive whole number. A fractional \code{n} is an error,
not a silent truncation, and the identical call in Python is refused the same way.}

\item{min, max}{the bounds for \code{rng_integers()}. \code{min} is included and \code{max} is EXCLUDED,
matching the ported Numerics \code{Next(minInclusive, maxExclusive)} a C# proposal is written
against, so \code{rng_integers(rng, 1, 0, 10)} draws one of \code{0:9}. Both must be whole numbers, and
the range between them can be at most 2147483647 wide (the ported generator draws an integer
span, and C# throws for a wider one).}
}
\value{
\code{rng_uniform()} a numeric vector of length \code{n} with values in \verb{[0, 1)};
\code{rng_integers()} an integer vector of length \code{n}.
}
\description{
Some corehydro verbs call a function you write and hand it the seeded random number generator
the run is using: the proposal function of a Gibbs sampler, the resample function of a
bootstrap. \code{rng_uniform()} and \code{rng_integers()} are how that generator is drawn from.
}
\details{
Use them for every random number your callback needs. Reaching for R's own generator
(\code{\link[stats:runif]{stats::runif()}}, \code{\link[base:sample]{base::sample()}}) instead is not an error and will not be caught, but it
silently breaks two guarantees corehydro otherwise makes: the run stops being reproducible from
its seed, and it stops agreeing with the identical run in Python. The handle draws from the same
Mersenne Twister the core seeded, so both hold.
}
\section{Lifetime}{

The handle borrows the generator for the duration of the one call it was given to. It is not an
object to keep. Storing it and drawing from it after your callback has returned raises an error
("this random number generator handle is no longer valid"), which is deliberate: the generator
it pointed at no longer exists, and reading it would crash the session rather than merely
misbehave.
}

\seealso{
\code{\link[stats:runif]{stats::runif()}} for ordinary R random numbers, which is what to use anywhere OUTSIDE
a corehydro callback.
}
