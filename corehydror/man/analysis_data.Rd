% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{analysis_data}
\alias{analysis_data}
\title{Censored observation data for an analysis}
\usage{
analysis_data(
  exact = NULL,
  interval = NULL,
  threshold = NULL,
  uncertain = NULL,
  low_outlier_threshold = NULL,
  mgbt_low_outliers = FALSE
)
}
\arguments{
\item{exact}{a numeric vector of observations, or a data frame with a \code{value} column plus
optional \code{index} and \code{is_low_outlier} columns.}

\item{interval}{a data frame with \code{lower}, \code{value}, and \code{upper} columns plus an optional
\code{index}, giving observations known only to lie in a range.}

\item{threshold}{a data frame with \code{start_index}, \code{end_index}, \code{value}, and \code{number_above}
columns, giving perception thresholds over spans of the record.}

\item{uncertain}{a list of \code{\link[=distribution]{distribution()}} objects (one per observation), or a list with an
\code{index} element and a \code{distribution} element holding them.}

\item{low_outlier_threshold}{optional numeric; values at or below it are treated as
left-censored low outliers.}

\item{mgbt_low_outliers}{logical; when \code{TRUE} the Multiple Grubbs-Beck test picks the low
outlier threshold from the data. Mutually exclusive with \code{low_outlier_threshold} and with
explicit \code{is_low_outlier} flags.}
}
\value{
An object of class \code{corehydro_data}.
}
\description{
Assemble the observations an analysis or model is fit to. Beyond a plain systematic record
(\code{exact}), a frame can carry the censored observation types Bulletin 17C flood-frequency work
depends on: historical or paleoflood observations known only within a range (\code{interval}),
perception thresholds recording that nothing above a level occurred over a span of years
(\code{threshold}), and observations whose measurement error is itself a distribution
(\code{uncertain}).
}
\details{
This is the port of the RMC.BestFit \code{DataFrame} class. It is named \code{analysis_data()} rather
than \code{data_frame()} to avoid colliding with the R and pandas types of that name.

Indexes are 0-based, matching the C# library and the shared C++ core, and are generated
sequentially when not supplied. An index is a position in the record, so an interval
observation at index 40 sits after the 40th exact observation in chronological order.
}
\examples{
# A systematic record on its own.
peaks <- c(12500, 15300, 8900, 22100, 18700, 14200, 9800, 28500)
analysis_data(peaks)

# Adding two historical floods known only within a range, and a perception threshold
# covering the 40 years before the gauge was installed.
analysis_data(
  exact = peaks,
  interval = data.frame(
    index = c(8, 9), lower = c(30000, 26000),
    value = c(35000, 29000), upper = c(40000, 32000)
  ),
  threshold = data.frame(
    start_index = 8, end_index = 47, value = 25000, number_above = 2
  )
)
}
\seealso{
\code{\link[=analysis_data_summary]{analysis_data_summary()}} for plotting positions and record diagnostics,
\code{\link[=model_univariate]{model_univariate()}} to fit a model to one, \code{\link[=mgbt_test]{mgbt_test()}}.
}
