% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toolbox.R
\name{uncertain_curve_sample}
\alias{uncertain_curve_sample}
\title{Sample an uncertain paired curve}
\usage{
uncertain_curve_sample(
  x,
  distributions,
  probability = NULL,
  order_x = c("ascending", "descending", "none"),
  order_y = c("ascending", "descending", "none"),
  strict_x = TRUE,
  strict_y = TRUE
)
}
\arguments{
\item{x}{numeric vector of x positions, at least one element.}

\item{distributions}{a \code{\link[=distribution]{distribution()}} object, or a list of them the same length as \code{x} --
one distribution per x position. A single distribution is recycled across every \code{x}.}

\item{probability}{quantile in \verb{[0, 1]} to sample at; \code{NULL} (default) samples the mean.}

\item{order_x, order_y}{one of \code{"ascending"} (default), \code{"descending"}, or \code{"none"}.}

\item{strict_x, strict_y}{require x/y to strictly increase/decrease (per \code{order_x}/\code{order_y})
between consecutive ordinates. Default \code{TRUE}.}
}
\value{
a data frame with columns \code{x} and \code{y}.
}
\description{
Mirrors the C# \code{UncertainOrderedPairedData.CurveSample()}/\code{CurveSample(double)}: collapses a
curve whose Y-coordinate is a whole distribution at each x down to a plain x-y curve, either
at the distributions' means (\code{probability = NULL}, the default) or at a shared quantile
(\code{probability} in \verb{[0, 1]}).
}
\examples{
x <- c(1, 2, 3, 5)
d <- list(distribution("Triangular", c(1, 2, 3)), distribution("Triangular", c(2, 4, 5)),
          distribution("Triangular", c(6, 8, 12)), distribution("Triangular", c(13, 19, 20)))
uncertain_curve_sample(x, d, probability = 0.5)
}
