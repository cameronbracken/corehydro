% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{model_bivariate}
\alias{model_bivariate}
\title{Bivariate copula model}
\usage{
model_bivariate(
  marginal_x,
  marginal_y,
  copula = "Normal",
  estimation_method = "InferenceFromMargins",
  parameters = NULL,
  parameter_values = NULL,
  use_default_flat_priors = NULL
)
}
\arguments{
\item{marginal_x, marginal_y}{each a list with \code{family}, \code{data}, and \code{parameter_values}
elements describing a fixed marginal.}

\item{copula}{copula family name: \code{"Normal"} (the default), \code{"StudentT"}, \code{"Clayton"},
\code{"Frank"}, \code{"Gumbel"}, \code{"Joe"}, or \code{"AliMikhailHaq"}.}

\item{estimation_method}{\code{"InferenceFromMargins"} (the default), \code{"PseudoLikelihood"}, or
\code{"FullLikelihood"}.}

\item{parameters}{optional \code{\link[=model_parameter]{model_parameter()}} object or list of them, setting bounds, fixed
flags, priors, or starting values.}

\item{parameter_values}{optional numeric vector of all parameter values, applied last.}

\item{use_default_flat_priors}{logical; set \code{FALSE} alongside a custom prior.}
}
\value{
An object of class \code{corehydro_model}.
}
\description{
Two fixed univariate marginals coupled by a bivariate copula.
}
\note{
Fit an Archimedean copula (\code{"Clayton"}, \code{"Frank"}, \code{"Gumbel"}, \code{"Joe"},
\code{"AliMikhailHaq"}) with \code{optimizer = "DifferentialEvolution"}. \code{\link[=fit_mle]{fit_mle()}} and \code{\link[=fit_map]{fit_map()}}
default to \code{"NelderMead"}, whose start point is the midpoint of the copula's constraint
range: for Gumbel, whose range is \verb{\{1, 100\}}, that is theta 50.5, and the local search slides
to the lower bound and reports \code{Success} with the independence copula. On 150 Gumbel pairs
simulated at theta 3, \code{"NelderMead"} returned theta 1.0000 with a log-likelihood of 0.0000
while \code{"DifferentialEvolution"} returned theta 3.0334 at 110.5081. \code{"Powell"} errors on some
samples, so \code{"DifferentialEvolution"} is the recommendation rather than any global optimizer.
Elliptical copulas (\code{"Normal"}, \code{"StudentT"}) are not affected.
}
\examples{
x <- c(12, 15, 9, 22, 18, 14, 10, 28, 17, 11, 19, 13)
y <- c(2.1, 2.6, 1.7, 3.7, 3.1, 2.5, 1.9, 4.6, 3.0, 2.0, 3.3, 2.3)
model_bivariate(
  marginal_x = list(family = "Normal", data = x, parameter_values = c(mean(x), sd(x))),
  marginal_y = list(family = "Normal", data = y, parameter_values = c(mean(y), sd(y))),
  copula = "Normal"
)
}
\seealso{
\code{\link[=bivariate_analysis]{bivariate_analysis()}}, \code{\link[=coincident_frequency_analysis]{coincident_frequency_analysis()}}.
}
