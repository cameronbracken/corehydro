% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toolbox.R
\name{link_function}
\alias{link_function}
\title{Construct a link function}
\usage{
link_function(type, ..., inner = NULL)
}
\arguments{
\item{type}{one of \code{link_names()}, matched case-insensitively.}

\item{...}{named construction parameters for the parameterized links: \code{lambda} for
\code{"YeoJohnson"}; \code{gamma0}, \code{scale}, \code{epsilon}, \code{delta} for \code{"ASinH"}; \code{a} for \code{"SES"};
\code{sigma0}, \code{a}, \code{lambda} for \code{"LogSES"}; \code{sigma0}, \code{log_scale}, \code{epsilon}, \code{delta} for
\code{"LogASinH"}; \code{mu0}, \code{scale} for \code{"Centered"}. R spells the Yeo-Johnson/LogSES exponent
\code{lambda}; Python spells it \code{lambda_}, because \code{lambda} is a reserved word there (see
\code{corehydropy.toolbox.Link}).}

\item{inner}{a \code{corehydro_link} wrapped by \code{"Centered"}, ignored for every other type.}
}
\value{
a \code{corehydro_link} spec list.
}
\description{
Mirrors the seven Numerics link functions and the five RMC.BestFit-specific ones (the
BestFit factory's own YeoJohnson case routes to the Numerics class -- see
\code{best_fit_link_function_factory.hpp} -- so there is one \code{"YeoJohnson"}, not two). Six of the
twelve take construction parameters, passed through \code{...} by name. \code{type} is matched
case-insensitively (as \code{\link[=trend]{trend()}} already does) and normalized to its canonical spelling
before being sent to the C++ layer.
}
\examples{
l <- link_function("Log")
link(l, c(1, 10, 100))
link_inverse(l, c(0, 1, 2))
}
