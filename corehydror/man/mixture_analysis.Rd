% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{mixture_analysis}
\alias{mixture_analysis}
\title{Bayesian mixture-model frequency analysis}
\usage{
mixture_analysis(
  data,
  families,
  zero_inflated = FALSE,
  sampler = "DEMCz",
  iterations = 3000L,
  output_length = 10000L,
  credible_level = 0.9,
  seed = 12345L,
  exceedance_probabilities = NULL,
  thinning_interval = -1L
)
}
\arguments{
\item{data}{numeric vector of observations.}

\item{families}{character vector of component distribution family names (e.g. \code{c("Normal", "Normal")}).}

\item{zero_inflated}{logical; add a zero-inflation component (default \code{FALSE}).}

\item{sampler}{MCMC sampler: \code{"DEMCz"} (default), \code{"DEMCzs"}, \code{"ARWMH"}, or \code{"NUTS"}.}

\item{iterations}{number of post-warmup MCMC iterations.}

\item{output_length}{number of posterior samples used to build the credible band.}

\item{credible_level}{credible-interval width (e.g. \code{0.90} for a 90\% band).}

\item{seed}{PRNG seed for the sampler (fixed for reproducibility).}

\item{exceedance_probabilities}{optional numeric vector of exceedance probabilities at which to
tabulate the curve; when \code{NULL}, the 25 standard default ordinates are used. Must be in
\strong{ascending} order (as the defaults are); an unsorted vector fails validation and the
analysis reports itself as not valid.}

\item{thinning_interval}{MCMC thinning interval; \code{-1} (default) keeps the sampler's own default.}
}
\value{
A named list: \code{parameters}, \code{mode_curve}, \code{mean_curve}, \code{lower_ci}, \code{upper_ci}, and the
scalars \code{aic}, \code{bic}, \code{dic}, \code{rmse}.
}
\description{
Fit a finite mixture of 1-3 component distributions to a sample with a Bayesian MCMC analysis
and return the frequency (quantile) curve, credible band, and goodness-of-fit scalars. Wraps the
shared C++ \code{MixtureAnalysis}.
}
\examples{
x <- c(520, 580, 610, 700, 760, 850, 950, 5000, 5400, 5800, 6300, 6800)
fit <- mixture_analysis(x, c("Normal", "Normal"), iterations = 100, output_length = 200,
                        seed = 12345, thinning_interval = 1)
fit$aic
}
