% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toolbox.R
\name{autocorrelation}
\alias{autocorrelation}
\title{Autocorrelation, autocovariance, or partial autocorrelation function}
\usage{
autocorrelation(
  x,
  max_lag = NULL,
  type = c("correlation", "covariance", "partial"),
  confidence_level = 0.95
)
}
\arguments{
\item{x}{numeric vector, at least two elements.}

\item{max_lag}{maximum lag. \code{NULL} (the default) uses the C# default rule.}

\item{type}{one of \code{"correlation"} (the default), \code{"covariance"}, or \code{"partial"}.}

\item{confidence_level}{confidence level for the band. Default 0.95.}
}
\value{
a \code{corehydro_autocorrelation} list with \code{lag}, \code{value}, \code{type}, and the two-element
\code{ci} (\code{lower}, \code{upper}).
}
\description{
Mirrors the C# \code{Autocorrelation} class. Computes the requested function out to \code{max_lag}
(default \code{floor(min(10*log10(N), N-1))}, matching the C# default) and attaches the asymptotic
confidence band for ACF/PACF rho values at \code{confidence_level}.
}
\examples{
x <- c(5, 6, 4, 7, 3, 8, 2, 9, 1, 10, 5, 6, 4, 7, 3, 8, 2, 9, 1, 10)
autocorrelation(x, max_lag = 5)
}
