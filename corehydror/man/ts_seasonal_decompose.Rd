% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/timeseries.R
\name{ts_seasonal_decompose}
\alias{ts_seasonal_decompose}
\title{Seasonal decomposition}
\usage{
ts_seasonal_decompose(ts, period)
}
\arguments{
\item{ts}{a \code{corehydro_ts}.}

\item{period}{the seasonal period in ordinates (12 for monthly data with an annual cycle). The
series must span at least two complete periods.}
}
\value{
a data frame of \code{date}, \code{trend}, \code{seasonal} and \code{residual}, one row per input ordinate.
}
\description{
Classical additive decomposition into trend, seasonal and residual components, with the
seasonal part extracted by keeping only the harmonics of the seasonal frequency.
}
\details{
The trend is a centred moving average over \code{period} ordinates, so it -- and the residual with
it -- is undefined for the first \code{period - 1} ordinates, reported as \code{NA}. The seasonal
component is defined everywhere. Where all three are defined they add back to the original
value exactly.
}
\examples{
dates <- seq(as.Date("2000-01-01"), by = "month", length.out = 48)
value <- 100 + 0.5 * seq_along(dates) + 10 * sin(2 * pi * seq_along(dates) / 12)
head(ts_seasonal_decompose(time_series(dates, value, "one_month"), period = 12))
}
