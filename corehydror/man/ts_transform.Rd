% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/timeseries.R
\name{ts_transform}
\alias{ts_transform}
\title{Transform the values of a time series}
\usage{
ts_transform(ts, fun, constant = 0, power = 1, base = 10, indexes = NULL)
}
\arguments{
\item{ts}{a \code{corehydro_ts}.}

\item{fun}{one of \code{"add"}, \code{"subtract"}, \code{"multiply"}, \code{"divide"}, \code{"absolute_value"},
\code{"exponentiate"}, \code{"logarithm"}, \code{"inverse"}.}

\item{constant}{the operand for add / subtract / multiply / divide.}

\item{power}{the exponent for \code{"exponentiate"}. Default 1.}

\item{base}{the logarithm base. Default 10.}

\item{indexes}{optional 1-BASED ordinate positions to restrict the transformation to
(Python's \code{transform()} takes 0-based positions).}
}
\value{
a \code{corehydro_ts}.
}
\description{
The library's in-place value transformations, exposed as pure verbs: each returns a new series
rather than modifying \code{ts}.
}
\details{
Every transformation LEAVES A MISSING VALUE MISSING except two, which follow the library:
\code{"logarithm"} writes a missing value for any non-positive input, and \code{ts_standardize()}
propagates missing values through the subtraction.

\code{indexes} restricts the transformation to the given 1-based ordinate positions. Two
transformations behave differently from their siblings when an index is out of range, because
the library does: \code{"logarithm"} and \code{"inverse"} raise, while the others skip it.
}
\examples{
ts <- time_series(as.Date("2000-01-01") + 0:4, c(3, 1, 4, 1, 5))
ts_transform(ts, "multiply", constant = 10)
}
