% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcmc.R
\name{mcmc_sample}
\alias{mcmc_sample}
\title{Sample the posterior of a distribution's parameters by MCMC}
\usage{
mcmc_sample(
  data,
  distribution = "Normal",
  sampler = "RWMH",
  iterations = NULL,
  warmup = NULL,
  chains = NULL,
  thinning = NULL,
  output_length = NULL,
  seed = 12345,
  initialize = "MAP"
)
}
\arguments{
\item{data}{numeric vector of observations.}

\item{distribution}{the distribution family name; see
\code{\link[=distribution_names]{distribution_names()}}.}

\item{sampler}{one of \code{"RWMH"} (the default), \code{"ARWMH"}, \code{"DEMCz"},
\code{"DEMCzs"}, \code{"HMC"}, \code{"NUTS"}, or \code{"SNIS"}.}

\item{iterations}{iterations per chain (sampler default if \code{NULL}).}

\item{warmup}{warm-up iterations discarded from each chain (sampler
default if \code{NULL}).}

\item{chains}{number of chains (sampler default if \code{NULL}).}

\item{thinning}{thinning interval (sampler default if \code{NULL}).}

\item{output_length}{total number of retained draws across all chains
(sampler default, 10,000, if \code{NULL}). The ported sampler collects
\code{ceiling(output_length / chains)} draws per chain after the iteration
loop. The ported floor is 100 and it is refused below that.}

\item{seed}{PRNG seed; \code{12345} is the C# default.}

\item{initialize}{chain initialization: \code{"MAP"} (from the posterior-mode
estimate, the C# default) or \code{"Randomize"} (draws from the priors).}
}
\value{
A list with \code{parameters} (parameter names), \code{chains} (a list of
one draws-by-parameters matrix per chain), \code{acceptance_rates}, \code{map}
(parameter values at the posterior mode), \code{map_fitness},
\code{posterior_mean}, \code{posterior_sd}, \code{posterior_median},
\code{posterior_lower_ci}, \code{posterior_upper_ci}, \code{rhat}, and \code{ess} (all
per-parameter vectors).
}
\description{
Fits \code{distribution} to \code{data} with uniform priors spanning the family's
parameter constraints (exactly the C# \code{GetParameterConstraints} bounds) and
returns the full sampler output: per-chain draws, acceptance rates, the MAP
estimate, posterior summaries, and Gelman-Rubin / effective-sample-size
diagnostics. Seven of the eight ported samplers are available (Gibbs needs
a model-specific conditional proposal and is not exposed here).
}
\details{
Runs the ported serial chain driver with the C# default seed, so a seeded
run reproduces the C# sampler stream bit-for-bit (and matches \code{corehydropy}
exactly). The priors are always the constraint-based uniforms; custom
priors are not exposed -- use \code{\link[=univariate_analysis]{univariate_analysis()}} and friends for the
full Bayesian workflow.
}
\examples{
\donttest{
x <- dist_random(distribution("Normal", c(100, 15)), 200, seed = 42)
fit <- mcmc_sample(x, "Normal", sampler = "DEMCzs", iterations = 2000)
fit$posterior_mean
fit$rhat
}
}
